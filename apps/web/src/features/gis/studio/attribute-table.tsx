/**
 * Атрибутивная таблица активного слоя (P2-E02 S03): строки, связанные с картой
 * (выделение ↔ подсветка, фильтр по охвату). Нижняя панель студии: пока пуста.
 */
export function AttributeTable() {
  return null
}
