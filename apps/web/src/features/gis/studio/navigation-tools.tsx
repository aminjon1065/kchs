/**
 * Инструменты карты (P2-E02 S02): выделение щелчком и рамкой, идентификация,
 * измерение, закладки, поиск (геокодер, координаты). Состояние — `useStudio()`.
 * Слот тулбара студии: пока пуст.
 */
export function NavigationTools() {
  return null
}
