/** Атрибуты и история редактируемого объекта, модерация (P2-E03 S02–S03). Правая панель: пока пуста. */
export function EditPanel({ layerId: _layerId }: { layerId: string | null }) {
  return null
}
