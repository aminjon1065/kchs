import { config } from 'zod'

// CSP запрещает eval (script-src 'self', ADR-0043). zod при создании первой схемы
// объекта проверяет new Function для ускоренного разбора — браузер сообщает об
// этом как о нарушении CSP, даже если исключение перехвачено. Модуль импортируется
// в main.tsx первым: настройка должна действовать до создания схем контрактов.
config({ jitless: true })
