// Команды CLI пишут итог в терминал; журнал — только предупреждения и ошибки,
// если не передан --verbose. Модуль импортируется до первого чтения конфигурации.
if (!process.argv.includes('--verbose')) process.env.LOG_LEVEL = 'warn'
