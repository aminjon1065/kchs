# Записи архитектурных решений (ADR)

Каждое существенное решение — отдельный файл. Номера 0001–0024 — решения этапа проектирования; исполнитель продолжает нумерацию с 0025.

| № | Решение | Статус |
|---|---|---|
| 0001 | Модульный монолит на TypeScript + вычислительный движок на Python | Принято |
| 0002 | PostgreSQL + PostGIS как единая система записи; колоночный tier DuckDB/Parquet позже | Принято |
| 0003 | Универсальный реестр объектов | Принято |
| 0004 | Модель доступа: ACL + пространства + политики типов + политики строк/столбцов | Принято |
| 0005 | REST + OpenAPI из zod-контрактов (без tRPC/GraphQL) | Принято |
| 0006 | React SPA на Vite с TanStack Router (без Next.js/SSR) | Принято |
| 0007 | Оболочка «рабочее пространство»: вкладки, разделяемые панели, контекст-панель, палитра | Принято |
| 0008 | Собственный DataGrid на TanStack Table/Virtual; запасной путь — AG Grid Community | Принято |
| 0009 | Графики через собственный ChartSpec, рендер Apache ECharts | Принято |
| 0010 | MapLibre GL JS; тайлы MVT из PostGIS в приложении; PMTiles-базовые карты; Martin для S2 | Принято |
| 0011 | QuerySpec и язык выражений с компиляцией в SQL; сырой SQL через libpg-query | Принято |
| 0012 | Собственный лёгкий движок процессов (без Camunda/Temporal) | Принято |
| 0013 | Единые обсуждения: комментарии к объектам = сообщения чатов | Принято |
| 0014 | Transactional outbox + Redis Streams как шина событий (без Kafka) | Принято |
| 0015 | BullMQ как система заданий для TypeScript и Python | Принято |
| 0016 | LiveKit как медиасервер | Принято |
| 0017 | Meilisearch как поисковый индекс + pgvector для семантики | Принято |
| 0018 | Tiptap + Yjs/Hocuspocus для совместного редактирования текста | Принято |
| 0019 | Собственная аутентификация + OIDC/LDAP-интеграции (без Keycloak) | Принято |
| 0020 | Одна организация на инсталляцию (без многоарендности) | Принято |
| 0021 | Docker Compose для S1, Kubernetes/Helm для S2+ | Принято |
| 0022 | Серверный рендеринг отчётов, карт и печатных форм через Playwright | Принято |
| 0023 | Собственная дизайн-система на Tailwind + Radix (без MUI/Ant/Mantine) | Принято |
| 0024 | Монорепо pnpm + Turborepo; Biome + dependency-cruiser; локали ru/tg/en | Принято |
| 0025 | Расширения PostgreSQL в схеме `extensions` | Принято |
| 0026 | Собственный образ PostgreSQL с PostGIS и pgvector | Принято |
| 0027 | Отметка обработки события после успешного вызова подписчика | Принято |
| 0028 | ISO 8601 для `timestamptz` на уровне типа колонки | Принято |
| 0029 | Отдельная база `kchs_test` для интеграционных тестов | Принято |
| 0030 | Типизированный клиент из `packages/contracts`; OpenAPI — для внешних потребителей | Принято |
| 0031 | Порт справочника вместо обращения ядра к модулю идентификации | Принято |
| 0032 | Гостевая ссылка открывается по короткоживущему токену доступа | Принято |
| 0033 | Режим «от имени» — заголовок запроса, а не состояние сессии | Принято |
| 0034 | Доставка уведомлений почтой: немедленно и дайджестом | Принято |
| 0035 | У каждой очереди заданий один исполнитель: TypeScript-воркер или движок | Принято |
| 0036 | Задание ставится в транзакции и уходит в очередь после коммита | Принято |
| 0037 | Собственный переводчик в `@kchs/i18n` вместо i18next; подписи дизайн-системы из словарей | Принято |
| 0038 | Доска: нативное перетаскивание и перенос с клавиатуры | Принято |
| 0039 | SchemaForm — управляемая форма на валидации packages/fields, без react-hook-form | Принято |
| 0040 | Политика безопасности: системные настройки, кэш процесса, шлюз незавершённой настройки входа | Принято |
| 0041 | Импорт пользователей из Excel: разбор в движке, проверка и создание в воркере, временные пароли одноразовым файлом | Принято |
| 0042 | Вложения: закрытая системная папка и доступ через связь `attachment` | Принято |
| 0043 | Строгий CSP: nonce запроса от Caddy, без inline-скриптов и eval | Принято |
| 0044 | Установка: `kchs init`, образ api одним бандлом, web — единственная точка входа | Принято |
| 0045 | Наблюдаемость: OpenTelemetry по запросу, агент Alloy, бюджеты API профилем k6 | Принято |
| 0046 | Импорт датасета: движок нормализует файл, воркер загружает его в таблицу; очередь `data` | Принято |
| 0047 | Правка схемы датасета: неизменный ключ поля, смена типа одной перезаписью с отчётом | Принято |
| 0048 | Роль `kchs_query` читает только таблицы строк датасетов | Принято |
| 0049 | Палитра графиков `viz` (порядок и шаги тёмной темы проверены на цветослепоту), отрисовка и тултипы ECharts без встроенных стилей | Принято |
| 0050 | DataGrid: строки окнами через `getRow`, правка пакетами с откатом, модель столбцов TanStack Table | Принято |
| 0051 | Строки датасета: чтение через компилятор, правка с версией строки; данные ошибки для клиента | Принято |
| 0052 | Сырой SQL: переписывание по позициям разборщика, служебные имена CTE, повторный разбор | Принято |
| 0054 | Демо-данные фазы 1: генератор файлов в движке и манифест загрузки | Принято |
| 0055 | Политики строк и столбцов: общий формат фильтра, проверка компилятором, связь со схемой | Принято |
| 0056 | Экспорт датасета: задание очереди `exports`, права на момент выполнения, XLSX без библиотек | Принято |
| 0057 | Территории и справочники: модуль GIS, территории ответственности, подстановки в запросах | Принято |
| 0058 | Показатели: определение, окна периодов, один путь значения, плитка и TV-режим | Принято |
| 0059 | SQL-лаборатория в API и на экране: способность `data.sql`, имена датасетов, ошибки с позицией | Принято |
| 0060 | Задачи и поручения: одна сущность, участники через ACL, действия Входящих, системный датасет `tasks` | Принято |
| 0061 | Telegram-бот и «Спросить данные»: канал уведомлений модуля, модуль ИИ, план вместо QuerySpec | Принято |
| 0062 | Откат датасета к прежней версии: отмена по истории строк и импорту «дополнить» | Принято |
| 0063 | Демо-датасеты в сиде: задание генератора, загрузка конвейером импорта, флаг `--data` | Принято |
| 0064 | Слои, карты и векторные тайлы: объекты реестра, MVT через компилятор с политиками, кэш по версиям | Принято |
| 0065 | Компилятор стилей карт `@kchs/map-style`, шкалы карт в токенах, легенда | Принято |
| 0066 | Базовые карты: сборка PMTiles без морских полигонов, реестр `basemap`, стиль и прокси через API | Принято |
| 0067 | Границы территорий из OpenStreetMap и внутренний геокодер по справочнику | Принято |
| 0068 | Геоформаты через GDAL, пересчёт систем координат, предпросмотр изменений импорта и геоэкспорт | Принято |
| 0069 | Пространственный анализ: шаг `spatial` компилятора и объект «анализ» | Принято |
| 0072 | Карта-студия: `MapCanvas` в дизайн-системе, MapLibre GL 6, SDF-значки, состояние во вкладке | Принято |
| 0073 | Инструменты карты, атрибутивная таблица и связанные представления (ViewContext) | Принято |
| 0074 | Время на карте, печать карты (PNG/PDF на клиенте), карта на дашборде и в тетради | Принято |
| 0075 | Редактор стиля слоя: рабочая копия с предпросмотром тайлов, статистика классов на сервере, пресеты по семантике полей | Принято |
| 0076 | Правка объектов на карте: запись строки через слой, модерация в `feature_edits`, terra-draw | Принято |
| 0077 | Хороплет-мастер — анализ `choropleth`, паспорт территории — сборка модулем GIS, территория у задачи | Принято |
| 0078 | Отчёты: шаблон из блоков, страница печати со служебным токеном, рендер в движке, рассылка под правами каждого получателя | Принято |
| 0079 | Движок процессов: чистая модель переходов, исполнение в ядре, таймеры в базе, расширения модулей | Принято |
| 0080 | Документооборот, ядро: типы, журналы и нумерация, версии с PDF, грифы и допуски ядра, режим администратора | Принято |
| 0081 | Календарь: события — объекты реестра с повтором по стенным часам, приглашения во Входящих, напоминания очередью, ICS по токену, проекции сроков через реестр | Принято |
| 0082 | Поручения в полном режиме: срок рабочими днями, продление, части соисполнителей, руководитель исполнителя, напоминания без дублей, «Контроль» на системном датасете | Принято |
| 0083 | Маршруты документов на движке процессов: статусы хуками поставщика, заморозка версии, простая ЭП с хэшем, регистрация шагом, права участников политикой типа | Принято |
| 0084 | Резолюции → поручения, исполнение документа, ознакомление — механизм ядра (учёт шага маршрута `acknowledge`), действия с формой в карточке | Принято |
| 0085 | Печатные формы и штампы — реестр форм с HTML api и рендером движка, шаблоны DOCX в песочнице, водяной знак на копии, сравнение версий по словам | Принято |
| 0086 | Дела и архив, переписка и отправка исходящих, представления документов, «Канцелярия», демо-документы | Принято |
| 0087 | Конструктор маршрутов: определение — единственная правда, схема списком с группами, проверка сервером, справочник модулей | Принято |
| 0088 | ИИ в документах: реквизиты из скана с подтверждением, краткое содержание и черновик ответа, порог грифа установки | Принято |
| 0089 | Встречи: объект реестра с комнатой LiveKit, токены по правам участника, гость по ссылке | Принято |
| 0090 | Чаты: беседы поверх обсуждений ядра, права по видам бесед, непрочитанное по отметке прочтения, поиск сообщений отдельным индексом | Принято |
| 0091 | Комната встречи: свой клиент на livekit-client, комната ожидания, «Показать всем» | Принято |
| 0092 | Запись встречи: объект реестра рядом с встречей, Egress пишет в хранилище сам, расшифровка — заданием движка | Принято |
| 0093 | Протокол встречи: повестка и протокол — один совместный документ, подтверждение создаёт поручения | Принято |
| 0094 | Push-уведомления браузера: Web Push как канал ядра, подписка на устройство, манифест PWA | Принято |
| 0095 | База знаний: страница — совместный документ из блоков, версии снимками, чанки в отдельном индексе, порт семантики | Принято |
| 0096 | Правила автоматизации: модуль над шиной событий, служебный пользователь правила, единый планировщик | Принято |
| 0097 | Публичный API: токен с областями по тегу маршрута, вебхуки с подписью и повторами, объект `integration`, пакет конфигурации по стабильным ключам | Принято |
| 0098 | Каталог LDAP/AD, единый вход OIDC и ключи входа: один путь входа, план изменений перед синхронизацией, passkey как самостоятельный вход и как второй фактор | Принято |
| 0099 | Поиск по смыслу: векторы объектов в Postgres (pgvector), движок считает эмбеддинги, гибридная выдача с RRF | Принято |
| 0100 | Ассистент в контекстной панели: цикл «решение → инструмент → решение» поверх схемы ответа, инструменты только на чтение, действия — предложением | Принято |
| 0101 | Качество данных: правила в метаданных датасета, проверка по новой версии заданием, SQL из реестра полей | Принято |
| 0102 | Происхождение и влияние: обход графа зависимостей с проверкой видимости на каждом шаге | Принято |
| 0103 | Формы сбора данных: отдельный модуль над публичным API датасетов, отправка как объект учёта, контроль сдачи матрицей | Принято |
| 0104 | Алерты на показатели: один тик единого планировщика, значение под правами владельца, доставка подписчиком события | Принято |
| 0106 | Пайплайны преобразований: шаги компилируются в QuerySpec, прогон — задание с правами запустившего | Принято |
| 0107 | Внешние БД как источники датасетов: подключение — интеграция, выборка — объект `source`, загрузка — staging | Принято |
| 0108 | Внешние ГИС-службы: WMS и WMTS — виды базовой карты, WFS и ArcGIS REST — слой-ссылка и разовый импорт | Принято |
| 0109 | Колоночный tier: копия версии датасета в Parquet, тяжёлые агрегаты — DuckDB в движке | Принято |
| 0110 | deck.gl для больших слоёв карты: те же тайлы MVT, порог — настройка, откат на MapLibre | Принято |
| 0112 | Совместное редактирование офисных файлов: сессия на версию файла, отдельная страница редактора со своей CSP, сохранение обычной версией | Принято |
| 0113 | Регистрация входящих из почты: письмо — запись очереди, черновик — объект реестра, дедупликация по Message-ID | Принято |
| 0115 | Возможности установки: модуль объявляет флаг, маршрут закрывается по тегу ответом «не найдено» | Принято |
| 0116 | Брендирование: логотип значением настройки, акцент — из палитры дизайн-системы с проверкой контраста | Принято |
| 0117 | Резервные копии S1: `pg_dump` заданием платформы в бакет копий, проверку восстановлением отмечает человек | Принято |
| 0121 | Проверки безопасности в CI: ZAP baseline со списком разобранных правил, Trivy по образам, жёсткий порог `pnpm audit` | Принято |
| 0122 | Секрет в адресе запроса: адрес маскируется в журналах, трассах и ответах, а не переезжает в заголовок | Принято |
| 0123 | Проверка в момент действия, а не только при настройке | Принято |

## Шаблон

```
# NNNN. Название
Статус: Принято | Предложено | Заменено ADR-XXXX
Дата: YYYY-MM-DD

## Контекст
## Решение
## Альтернативы
## Последствия
```
