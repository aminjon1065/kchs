# Журнал реализации kchs

Заполняет исполнитель (Claude Code, Opus 5). Обновляется по завершении каждого эпика.

## Версии инструментов

Зафиксированы на старте фазы 0 (18.09.2026), дополнены при её закрытии:

| Инструмент | Версия |
|---|---|
| Node.js | 22.23.2 |
| pnpm | 12.4.2 (workspaces + `allowBuilds`) |
| TypeScript | 5.9.3 (strict, `noUncheckedIndexedAccess`) |
| Turborepo | 2.10.13 |
| Biome | 2.5.14 |
| knip | 5.66 (мёртвый код и зависимости) |
| esbuild | 0.28.2 (бандл api/worker и CLI `kchs` для образа, ADR-0044) |
| PostgreSQL | 17 + PostGIS 3.5 + pgvector (собственный образ `kchs/postgres:17-3.5`) |
| Redis | 7 (alpine) |
| MinIO | RELEASE.2025-09-07 (quay.io) |
| Meilisearch | 1.53 |
| Fastify | 5.6 + `fastify-type-provider-zod` 5 |
| Drizzle ORM | 0.44 + Drizzle Kit 0.31 |
| zod | 4.1 |
| BullMQ | 5.63 |
| Socket.IO | 4.8 + Redis-адаптер |
| OpenTelemetry | API 1.9, SDK Node — включается адресом OTLP (ADR-0045) |
| React | 19 + TanStack Query 5 (навигацию ведёт оболочка вкладок; TanStack Router не используется и удалён) |
| Tailwind CSS | 4 (`@theme inline`) + Radix Primitives |
| Storybook | 10.6 — визуальные снимки и axe в Linux-образе Playwright |
| Playwright | 1.56 |
| Python (движок) | 3.13 + FastAPI + bullmq (poppler, LibreOffice, tesseract) |
| Наблюдаемость | Alloy 1.12, Tempo 2.9, Loki 3.6, Prometheus 3.9, Grafana 12.3, k6 1.4 |
| Docker | 29.7.2, Compose v5.5.1 |

## Фаза 0 — Ядро платформы

Статус: **завершена 18.09.2026.** Приложение поднимается в контейнерах с нуля
(`docker compose --profile app up` + `kchs init` — 24 с до рабочей системы при бюджете 10 минут),
seed загружается, семь сценариев приёмки `04-verification.md` §3 проходят через интерфейс.

Честная оговорка. Первая запись этого журнала объявила фазу завершённой преждевременно:
при сверке с бэклогом и приёмочным прогоном через интерфейс нашлись пропущенные истории
(теги, политика безопасности, вложения, превью файлов, консоль ролей/пространств/объявлений,
настройка «Мой день», присутствие, реакции и вложения в сообщениях, установка `kchs init`,
наблюдаемость) и уязвимости доступа. Они закрыты в ветке `phase/0-closeout` — 69 коммитов
поверх исходного (`main`: один коммит «Initial commit»); ниже — состояние после закрытия.

| Эпик | Статус | Что сделано |
|---|---|---|
| P0-E01 Репозиторий, инфраструктура, CI | done | Монорепо pnpm+turbo; compose: данные (postgres/redis/minio/meilisearch/mailpit), профили `app` (api/worker/engine/web — Caddy со SPA, CSP), `media`, `observability`; `kchs init` (ADR-0044), `verify-stack.sh` — установка с нуля на чистых томах; CI: статика с knip, тесты, e2e с движком, ночная установка в контейнерах и замер k6; OpenAPI собирается без сервера (ADR-0030); OpenTelemetry по запросу (ADR-0045) |
| P0-E02 База данных и миграции | done | Миграции с нуля идемпотентны, advisory lock, 5 ролей БД с минимальными привилегиями, `audit_log` партиционирован и неизменяем; каталог миграций находится и из бандла |
| P0-E03 Идентификация | done | Argon2id, сессии + CSRF, блокировка после 10 попыток, MFA TOTP + коды восстановления, восстановление доступа; политика безопасности — обязательная MFA по ролям, простой сессии (ADR-0040); лимиты входа по адресу и логину (NAT организации) |
| P0-E04 Организация, роли, делегирование | done | Дерево подразделений, должности, группы, 7 системных ролей; импорт пользователей из Excel с отчётом по строкам (ADR-0041); из консоли — создание сотрудников и подразделений, роли, временный пароль, сброс MFA, блокировка; замещение назначается и завершается из профиля |
| P0-E05 Пространства и реестр объектов | done | `ObjectService`, реестр типов, предки, корзина/архив, избранное, недавние (обрезка до 200 — ночным заданием); теги со словарём пространства; участники пространства из интерфейса, в пространстве всегда остаётся администратор |
| P0-E06 Доступ | done | `authorize()` по источникам доступа, политики типов, `visibleObjectsSql`, «Объяснить доступ», разрыв наследования закрывает всё поддерево; гостевые ссылки в диалоге «Поделиться» (пароль, срок, счётчик, лимит открытий на ссылку); вложения — доступ через объект (ADR-0042); матрица негативных тестов по реестру типов |
| P0-E07 События, задания, realtime | done | Outbox в одной транзакции, повторная доставка брошенных событий, проверка каталога; задания ставятся в транзакции (ADR-0036), один исполнитель у очереди (ADR-0035); Socket.IO с проверкой прав комнат; присутствие — кто смотрит файл |
| P0-E08 Обсуждения | done | Беседы объектов, треды (модель), упоминания с выбором сотрудника по «@», реакции, вложения в сообщениях; правки/удаления/реакции доходят до соседей в реальном времени |
| P0-E09 Уведомления и Входящие | done | Агрегация, каналы, почта (сразу и дайджестом, ADR-0034), Входящие с замещением; упоминание не раскрывает объект тому, у кого нет доступа |
| P0-E10 Поиск | done | Meilisearch с `aclPrincipals`, переиндексация поддерева при смене доступа, палитра команд, страница поиска с фасетами; файлы ищет каждый, кто их видит (раньше — только администратор) |
| P0-E11 Файлы | done | Загрузка в S3 (multipart + докачка), версии, скачивание по подписанным ссылкам (подпись — публичным адресом хранилища); превью изображений/PDF/офиса и извлечение текста в движке; вложения объектов |
| P0-E12 Поля, формы, коллекции | done | `packages/fields`, `SchemaForm` без react-hook-form (ADR-0039), `CollectionView`, `FilterBuilder`, сохранённые представления |
| P0-E13 Дизайн-система | done | Токены → CSS-переменные, две темы, 3 плотности; Storybook, визуальные снимки и axe в двух темах; свои шрифты с таджикскими буквами; вертикальные вкладки; подтверждения — `alertdialog`; тексты только из словарей (ADR-0037) |
| P0-E14 Оболочка рабочего пространства | done | Рельс, навигатор, вкладки с предпросмотром и разделением, контекстная панель, палитра, строка состояния, мобильная раскладка; именованные рабочие пространства; строгий CSP (ADR-0043) |
| P0-E15 «Мой день» и администрирование | done | «Мой день»: Входящие, Объявления, Продолжить (сохранённые рабочие пространства), Недавние, Закреплённое; настройка виджетов и набор по роли. Консоль: здоровье, пользователи, оргструктура, роли и способности, пространства, объявления, аудит с фильтрами и CSV, безопасность |
| P0-E16 Приёмка фазы | done | Seed демо-организации; e2e: 35 сценариев, из них приёмка фазы 0 целиком через интерфейс (`acceptance-p0.spec.ts`); дымовой прогон API 41/41 — и на стенде разработки, и через web установки в контейнерах |

### Как запустить

Разработка (инфраструктура в Docker, api/worker и web на хосте):

```bash
bash infra/scripts/generate-secrets.sh
pnpm install && docker compose up -d --wait
pnpm db:migrate && pnpm db:seed && pnpm dev
```

Установка целиком в контейнерах (ADR-0044):

```bash
bash infra/scripts/generate-secrets.sh --mode app
docker compose --profile app up -d --build --wait
docker compose exec api kchs init --admin-email admin@example.org
docker compose exec api kchs seed        # демо-данные, по желанию
```

Демо-данные: администратор `admin`, сотрудники `user001…user060` (пароли — N2 в
`05-risks-and-open-questions.md`); почта разработки — mailpit.

### Результаты проверок (18.09.2026, вершина `phase/0-closeout`)

| Проверка | Команда | Результат |
|---|---|---|
| Статический анализ | `pnpm lint` | 404 файла, ошибок нет |
| Типы | `pnpm typecheck` | 6 пакетов из 6 |
| Мёртвый код | `pnpm knip` | находок нет |
| Границы модулей | `pnpm deps:check` | нарушений нет (231 модуль, 987 зависимостей) |
| Unit-тесты | `pnpm test` | 75 (api 13, contracts 8, fields 23, i18n 9, ui 7, web 15) |
| Интеграционные тесты | `pnpm test:integration` | 213 тестов в 20 файлах |
| Сквозные сценарии | `pnpm e2e` | 35 сценариев в 16 файлах, в т. ч. приёмка фазы 0 и мобильная раскладка |
| Дизайн-система | `pnpm --filter @kchs/ui test:visual` | 201 проверка: снимки историй в двух темах и axe |
| Дымовой прогон API | `bash infra/scripts/smoke-api.sh` | 41 из 41 |
| Установка с нуля | `bash infra/scripts/verify-stack.sh` | up до готовности 23 с, `kchs init` 1 с, демо-данные 5 с; дымовой прогон через web 41/41; браузер: вход, загрузка в S3, превью движка, CSP без нарушений |
| p95 API | `bash infra/perf/run-k6.sh` | всё в бюджете: чтение/запись 8–25 мс при бюджете 200, поиск 12 мс при 100, вход 83 мс при 500; ошибок 0 % |
| Словари | `pnpm i18n:check` | ru 100 % (1054), en 100 %, tg 24 % |
| Контраст WCAG 2.2 AA | `pnpm --filter @kchs/ui contrast` | обе темы в норме |

### Сценарии приёмки фазы 0

`04-verification.md` §3 — чем подтверждён каждый сценарий:

| Сценарий | Чем проверен |
|---|---|
| 1. Администратор входит с MFA, создаёт подразделение и пользователя, назначает роль | `e2e/acceptance-p0.spec.ts` (через интерфейс), `test/acceptance.test.ts` |
| 2. Сотрудник видит «Мой день», создаёт пространство «Проект», приглашает коллегу редактором | `e2e/acceptance-p0.spec.ts`, `test/acceptance.test.ts` |
| 3. PDF 50 МБ в папке, коллега видит превью, упоминание → уведомление и письмо | `e2e/acceptance-p0.spec.ts` — письмо именно об этом упоминании в mailpit |
| 4. Разрыв наследования, ссылка с паролем для третьего, четвёртый не находит файл и получает 404 | `e2e/acceptance-p0.spec.ts` — четвёртый принят в пространство уже после разрыва, коллега с тем же запросом файл находит |
| 5. Пять вкладок, разделение, перезагрузка | `e2e/acceptance-p0.spec.ts`, `e2e/workspace.spec.ts` |
| 6. Замещение на неделю, «от имени», аудит с обеими записями | `e2e/acceptance-p0.spec.ts` (скачивание «от имени», аудит); элемент Входящих заместителю — `test/acceptance.test.ts`: в фазе 0 элементы Входящих ещё никто не создаёт |
| 7. Аудит и здоровье; worker убит во время задания | `e2e/acceptance-p0.spec.ts` (аудит, здоровье); повтор задания и сохранность события — `test/events.test.ts`, `test/jobs.test.ts` |

### Найдено и исправлено при закрытии фазы

Безопасность:

- Вложение сообщения обсуждения открывало автору чужой файл: связь `attachment` создавалась без
  проверки прав на файл, а по ADR-0042 она открывает файл читателям объекта (89c6572).
- Шлюз MFA обходился: коррелированный подзапрос Drizzle в списке выборки сравнивал столбец сам с собой.
- Повышение привилегий через управление ролями, захват учётной записи через восстановление,
  вход с неподтверждённой настройкой (2274d59).
- Разрыв наследования не закрывал поддерево: роль пространства, ACL предков, вложенные копии (0f9d0e9);
  утечки по матрице типов объектов (a96dcb2).
- Уведомление об упоминании раскрывало объект получателю без доступа (14f64bf).
- Внутренние маршруты движка были доступны через прокси (fddbe19).

Работоспособность:

- Лимит входа по одному адресу блокировал организацию за NAT после десятого входа за минуту (709e300);
  429 приходил английским текстом плагина.
- Файлы в поиске находил только администратор (5230c10).
- Подписанные ссылки S3 не работали в контейнерах: адрес подменялся после подписи (d061285).
- Реакции писались мимо outbox, правки и реакции не доходили до соседей (89c6572); список сообщений
  с вложением падал на схеме ответа.
- Из пространства можно было убрать последнего администратора (50d32f7).
- Обрезка «недавних» была написана, но не вызывалась — таблица росла без предела (knip).
- Брошенные события не доставлялись повторно (5bf0129); задание могло уйти в очередь до коммита (c1de5aa).
- Сбой отправки комментария был молчаливым и терял текст (a157c58).

### Принятые решения (ADR)

- ADR-0025…0034 — см. предыдущие записи (расширения в `extensions`, свой образ Postgres,
  отметка после подписчика, ISO-время, тестовая база, типизированный клиент, порт справочника,
  токен гостевой ссылки, «от имени» заголовком, почта сразу и дайджестом).
- ADR-0035 — у каждой очереди заданий один исполнитель.
- ADR-0036 — задание ставится в транзакции и уходит в очередь после коммита.
- ADR-0037 — собственный переводчик в `@kchs/i18n` вместо i18next.
- ADR-0038 — нативное перетаскивание и перенос с клавиатуры.
- ADR-0039 — SchemaForm без react-hook-form.
- ADR-0040 — политика безопасности: системные настройки, кэш процесса, шлюз незавершённой настройки входа.
- ADR-0041 — импорт пользователей из Excel.
- ADR-0042 — вложения: закрытая системная папка и доступ через связь `attachment`.
- ADR-0043 — строгий CSP с nonce запроса.
- ADR-0044 — установка: `kchs init`, образ api одним бандлом, web — единственная точка входа.
- ADR-0045 — наблюдаемость: OpenTelemetry по запросу, Alloy, бюджеты API профилем k6.

## Фаза 1 — Данные и аналитика

Ветка `phase/1-data`, начата 18.09.2026. **Все семь сценариев приёмки фазы проходят в e2e, демо-
датасеты на 5 млн строк загружаются сидом, бюджет запросов выдержан (k6).** Эпики P1-E01…P1-E10
закрыты в объёме приёмки; отложенное — ниже.

**Сделано (в ветке):**

- Контракты фазы: датасет, импорт, QuerySpec, ChartSpec, дашборд; события `dataset.*`,
  `chart.updated`, `dashboard.updated`; очередь `data`.
- P1-E01 — датасет как объект реестра; таблицы `ds.t_*`, история `ds.h_*`, версии; правка схемы с
  пробным отчётом смены типа (ADR-0047); `kchs_query` читает только таблицы строк (ADR-0048);
  строки: чтение через компилятор с политиками, правка с `_ver` и 409, история (ADR-0051).
- P1-E02 — анализ и нормализация в движке (CSV/TSV/XLSX/XLS/JSON/NDJSON/GeoJSON, кодировки,
  локали, геометрия; 1 млн строк CSV ≈ 9 с), загрузка воркером (replace/append/upsert/sync),
  мастер импорта в 4 шага.
- P1-E03 — экран датасета: DataGrid (ADR-0050) с поиском, сортировкой и правкой ячеек, редактор
  схемы, профиль столбца, версии, импорты; слой `DatasetAccess` — политики строк и столбцов.
- P1-E04 — компилятор `packages/query` (выражения, QuerySpec → SQL, политики, 616 тестов),
  выполнение под `kchs_query` с тайм-аутом и кэшем Redis по версиям.
- P1-E05 S01 — «Исследование»: фильтры, разрезы с интервалами, меры, сортировка, лимит, автозапуск.
- P1-E06 S01, S03 (часть) — `chart-spec` и `Chart` (ADR-0049), графики и дашборды — объекты
  реестра; данные — с правами смотрящего, плитка без доступа — «нет доступа».
- Попутно: доступность панели вкладок и пустой таблицы (axe чистый), имя автора в письмах
  уведомлений, устойчивость e2e к данным общего стенда.

- Политики строк и столбцов (ADR-0055): API для `manage+`, фильтр проверяется компилятором,
  событие `dataset.policies_changed`, вкладка «Доступ» с выбором принципала и конструктором
  фильтра; поле из политики не удаляется и не меняет тип на несовместимый.
- P1-E03 S01 — добавление строк вставкой из буфера (ниже последней строки и «Добавить строки»);
  S03 — карточка строки: поля с правкой по месту, история, удаление, новая строка формой;
  S04 — экспорт CSV/XLSX/JSON/GeoJSON заданием `exports` с политиками на момент выполнения,
  до 1 млн строк, XLSX собственным потоковым писателем (ADR-0056).
- P1-E04 S04 и P1-E05 S02 — сырой SQL (libpg-query, белые списки, подзапросы-политики,
  ADR-0052), SqlEditor на CodeMirror 6 и экран «SQL-лаборатория» (способность `data.sql`,
  ошибки с позицией, ADR-0059).
- P1-E06 S02 — показатели: определение, окна периодов, сравнения, пороги, один путь значения,
  плитка-показатель и TV-режим дашборда (ADR-0058).
- P1-E10 (часть) — детерминированный генератор демо-данных в движке: территории, справочники,
  5 млн происшествий, гидропосты, объекты и зоны риска, манифест загрузки (ADR-0054).
- Безопасность: пользовательские запросы к данным без подмены роли `kchs_query` ролью приложения
  (в продакшене конфигурация без `DATABASE_QUERY_URL` не загружается); поиск принципалов больше
  не находит заблокированных по логину; удаление обычного поля датасета больше не падает с 500.
- P1-E06 S03 — детализация плитки дашборда до строк (условия графика, фильтры дашборда и
  выбранный элемент, политики смотрящего) и перекрёстный фильтр по привязке плитки.
- P1-E01 S04 — откат датасета к прежней версии новой версией «Откат» (ADR-0062): история строк
  помечается номером версии, отменяются правки и импорт «дополнить»; замена, синхронизация и
  смена схемы откат блокируют с понятной причиной. Попутно исправлен порядок истории строки
  (после девятой записи сортировка шла по тексту).
- P1-E07 — справочник территорий (модуль GIS, загрузка в seed, экран «Территории», пикер-дерево),
  поле-территория с вводом кодом или названием и при импорте, функции `territory_level`,
  `territory_name`, `lookup_label`, `within` с дочерними, `@my_territories` из территорий
  подразделений — во всех путях к данным, включая SQL-лабораторию; подписи территорий и
  справочников в гриде, графиках и плитках (ADR-0057).
- P1-E08 и P1-E04 S05 — задачи, проекты и поручения: объекты реестра, ключи без пропусков,
  участники через ACL, поручение «назначено → принято → отчёт → принято/возвращено» действиями
  Входящих (`POST /inbox/:id/act`), поручение из строки датасета, доска, «Мои задачи»;
  системный датасет `system:tasks` с правами смотрящего (ADR-0060).
- P1-E09 — Telegram-бот (долгий опрос в роли worker, привязка одноразовой ссылкой, канал
  уведомлений ядра; поручения и упоминания приходят сразу) и модуль ИИ: «Спросить данные» —
  вопрос → план по видимой схеме → запрос с политиками пользователя в «Исследовании»; во внешний
  провайдер уходят только вопрос и видимая схема, ИИ по умолчанию выключен (ADR-0061).
- P1-E10 — демо-датасеты в сиде: `pnpm db:seed --data=small|demo` ставит задание генератора в
  движке и грузит наборы тем же конвейером импорта (ADR-0063); «Территория» в демо-данных —
  поле-территория; k6-профиль запросов `infra/perf/k6/data-queries.js`; e2e всех сценариев.
- Для приёмки доделаны: вычисляемая мера «Формула» в «Исследовании», фильтр дашборда
  «Территория» (дерево справочника, с вложенными) и перекрёстный фильтр по территории;
  поручения по умолчанию приходят в Telegram.
- Исправлено по ходу: сортировка «Исследования» оставалась на пропавшем столбце после смены меры
  (запрос падал) и шла по исходному полю у разреза с интервалом; `options.cache: false`
  контракта QuerySpec не учитывался; порядок истории строки после девятой записи.

**Приёмка фазы 1 (19.09.2026, стенд разработки):**

| № | Сценарий (04-verification.md) | Проверка | Итог |
|---|---|---|---|
| 1 | XLSX из 3 листов, «грязные» даты и числа → версия и профиль; 1 млн строк | e2e `data-import-xlsx`, `data-import`; 5 млн строк — сид `--data=demo` (импорт ≈ 2,5 мин), движок 1 млн ≈ 9 с | ✓ |
| 2 | Справочник, связь поля, подписи, 3 правки, 100 строк из буфера, отмена | e2e `data-reference-grid` | ✓ |
| 3 | Период, территория × месяц, вычисляемая мера → график → дашборд с «Территорией» и детализацией | e2e `data-dashboard-drill`, `data-analytics` | ✓ |
| 4 | Показатель с порогом, сравнение с прошлым периодом, TV | e2e `data-metrics` | ✓ |
| 5 | Руководитель «только своя область»: дашборд, экспорт, SQL, `public.users` отклонён | e2e `data-policies` | ✓ |
| 6 | Поручение из строки → Telegram по ссылке → отчёт → приёмка, Входящие закрыты | e2e `tasks-instructions`; Telegram — интеграционный `telegram` (поддельный Bot API) | ✓ |
| 7 | «Спросить данные»: вопрос → график и запрос → правка вручную | e2e `data-ask` с поддельным провайдером (в CI тоже) | ✓ |

Критерии roadmap: p95 запросов ≤ 2 с на 5 млн строк — k6 `data-queries` (2 запроса/с, 2 мин):
худший вид, ряд по месяцам и типам, p95 1,0 с; сводка по областям за период 0,18 с, страница
таблицы 0,07 с, детализация 0,05 с, профиль столбца 0,04 с. Негативные тесты политик строк — через
грид, экспорт, SQL, график, дашборд, детализацию, показатели и ИИ (`data-policies`, `data-rows`,
`data-export`, `data-sql`, `data-charts`, `data-metrics`, `data-ask`, `data-territories`).
Визуальные тесты графиков — в наборе дизайн-системы.

**Проверки на вершине ветки (19.09.2026):** интеграционные 438, модульные api 43 / web 43 /
ui 48 / fields 46 / chart-spec 53 / contracts 8 / i18n 9, query 782 (с Postgres), движок 120,
визуальные с axe 296, e2e 45 — зелёные; lint, typecheck, deps:check, knip, i18n — чистые.

**Дальше:** фаза 2 (GIS, тетради, отчёты) по roadmap; до неё — по желанию владельца: подписи
территорий и справочников в карточке строки, просмотр строк «как было», шаблоны импорта.

**Отложено:** шаблоны импорта (P1-E02 S02), смена типа крупных таблиц заданием (ADR-0047),
объединение правок в одну версию (ADR-0051), фасеты и экспорт изображений графиков, серверная
история и сохранение запросов SQL-лаборатории (ADR-0059), «Сохранить как показатель» из
«Исследования», просмотр строк «как было» на версии (ADR-0062); по задачам — сроки в рабочих
днях, напоминания и `task.overdue`, запрос продления, подзадачи и вехи, правка рабочего
процесса проекта в интерфейсе, системный источник в «Исследовании» (ADR-0060); по Telegram и
ИИ — кнопки действий в сообщениях, повторная доставка, вебхук, промпты в файлах с golden-тестами
(ADR-0061).

## Фаза 2 — GIS, тетради, отчёты
_(P2-E01…P2-E06 — не начата)_

## Фаза 3 — Документооборот, поручения, календарь
_(P3-E01…P3-E05 — не начата)_

## Фаза 4 — Чаты, встречи, протоколы
_(P4-E01…P4-E03 — не начата)_

## Фаза 5 — Знания, автоматизация, интеграции, ИИ, масштаб
_(P5-E01…P5-E09 — не начата)_

## Отклонения от проекта

Все отклонения оформлены ADR (см. `docs/adr/`), ни одно не меняет решения со статусом «Решено».

## Открытые вопросы к владельцу продукта

`04-delivery/05-risks-and-open-questions.md`, раздел «Новые вопросы»; работа продолжается
с рабочим допущением:

- **N2** — учётные данные демо-стенда (`SEED_ADMIN_PASSWORD` / `SEED_USER_PASSWORD`).
- **N3** — обязательность MFA по умолчанию: механизм есть (ADR-0040), открыто значение по умолчанию.
- **N4** — производственный календарь РТ: `kchs init` заносит праздники с постоянной датой,
  Иди Рамазон и Иди Қурбон вносит администратор.
- **N5, N6** — к фазе 1: источник территорий и классификатор кодов (Q6), политика облачного ИИ (Q7).

N1 (git недоступен) снят: история ведётся, фаза 0 — в ветке `phase/0-closeout`.

## Известные ограничения

- Архивирование пространства не сделано: содержимое не лежит под пространством в `object_ancestors`,
  заморозка только объекта пространства оставила бы содержимое изменяемым — нужен отдельный проект
  (архив по `space_id` с восстановлением по отметке времени).
- Способности системных ролей только читаются; свои роли (`roles.manage`) — позже.
- Таджикская локаль покрыта на 24 % ключей (недостающие берутся из русской) — нужна вычитка носителем.
- Наблюдаемость: нет экспортёров метрик Postgres/Redis/MinIO/Meilisearch, Alertmanager (правила есть,
  доставки нет), трасс движка; экран «Здоровье» пока не читает метрики.
- Производительность, замеченная при профилировании: каждый запрос обновляет сессию (`touchSession`),
  `GET /objects/:id` проверяет доступ дважды — в бюджете, но стоит поправить до фазы 1 под нагрузкой.
- Поле даты-времени `SchemaForm` и формы объявлений работают в поясе браузера, а не профиля пользователя.
- Сборка web — один бандл больше 500 кБ; разбиение по экранам — вместе с модулями фазы 1.
- Партиционирование `activities`, `notifications`, `query_runs` — до появления объёмов.
- Заголовки карточек дизайн-системы — не заголовки (`div`), навигация скринридером по виджетам
  «Мой день» — по тексту.
- Мягко удалённая строка датасета держит значение ключа (уникальный индекс полный): новая строка с
  тем же ключом отклоняется, откат, возвращающий занятый ключ, — тоже (409).

## Следующий конкретный шаг

Фаза 1 принята на стенде разработки (все семь сценариев, бюджет запросов, негативные тесты
политик). Следующая — фаза 2 «GIS, тетради, отчёты»: начать с P2-E01 (стили, MVT-тайлы с политиками
строк) на демо-слоях «Объекты защиты» и «Зоны риска».
