# Риски и открытые решения

## 1. Архитектурные и технические риски

| # | Риск | Влияние | Митигация |
|---|---|---|---|
| R1 | Широта продукта → незавершённые модули | потеря целостности | ядро первым; фазы с DoD; вертикальные срезы; никаких параллельных «полустроек» |
| R2 | Компилятор запросов: ошибки типизации/SQL, обход политик строк | безопасность, доверие | golden-тесты; единственный путь к данным; libpg-query для SQL; роль `kchs_query`; негативные тесты по всем путям |
| R3 | Производительность собственного DataGrid | UX аналитика | ранние бенчмарки (P1-E03); запасной путь — AG Grid Community за тем же интерфейсом (ADR-0008) |
| R4 | Тайлы «на лету» на больших слоях | медленные карты | генерализация, кластеризация, кэш, Martin в S2, deck.gl |
| R5 | Совместное редактирование (Yjs) и права | конфликты, утечки | ограниченный набор объектов; права на уровне Hocuspocus; тесты |
| R6 | LiveKit: сеть/NAT/TURN, запись | звонки не работают в части сетей | TURN/TLS на 443; предварительная проверка сети в клиенте; документ по портам для ИТ-службы |
| R7 | Качество расшифровки таджикской речи | протоколы неточны | ручной режим; выбор модели; оценка на пилоте; возможна дообученная модель |
| R8 | LLM-провайдер: стоимость, недоступность, приватность | функции ИИ | сменяемый провайдер; on-prem вариант; кэш; лимиты; всё ИИ — опционально |
| R9 | Один Postgres для всего | узкое место при росте | партиционирование, реплика чтения, колоночный tier; мониторинг с ранними порогами |
| R10 | Движок процессов: сложные маршруты, долгие ожидания | баги оркестрации | событийная модель шагов, идемпотентность, тесты восстановления; Temporal как план Б |
| R11 | Модель доступа с политиками типов — сложность | ошибки прав | таблицы истинности, генерируемые негативные тесты, «объяснить доступ» как инструмент отладки |
| R12 | Локализация таджикского (терминология, ИИ, OCR) | принятие пользователями | словарь терминов в глоссарии с ранней проверкой носителем; ключи с fallback на ru |
| R13 | Геометрии административных границ: источник и лицензия | юридический | зафиксировать источник в seeds/README, предпочесть официальные данные |
| R14 | Электронная подпись: юридическая сила | документооборот «не считается» | порт `SignatureProvider`; простая ЭП как внутренний инструмент; уточнить требования |
| R15 | Объём фронтенда (десятки экранов) | производительность, время | ленивые модули, единые компоненты, бюджеты бандла |
| R16 | Python-движок: тяжёлый образ, модели | развёртывание | образ с моделями отдельным слоем; профили compose; документ по ресурсам |

## 2. Открытые продуктовые вопросы (с рабочим вариантом)

| # | Вопрос | Рабочий вариант | Кому/когда уточнить |
|---|---|---|---|
| Q1 | **Ответ владельца 24.09.2026 — §5.** Предметная область — КЧС? Какие типы ЧС, источники, формы? | платформа нейтральна; предметный пакет — конфигурацией (фаза 5) | владелец продукта до фазы 2 (влияет на seed и территории) |
| Q2 | Одна организация на инсталляцию? | да | до фазы 0 (влияет на модель) |
| Q3 | **Ответ владельца 24.09.2026 — §5.** Языки и приоритет таджикского | ru основной, tg/en | до фазы 0 |
| Q4 | **Ответ владельца 24.09.2026 — §5.** Требования к ЭЦП | простая ЭП, порт для квалифицированной | до фазы 3 |
| Q5 | **Ответ владельца 24.09.2026 — §5.** Официальные формы журналов, печатных форм, номенклатуры дел | стандартные CIS-практики | до фазы 3 |
| Q6 | **Ответ владельца 24.09.2026 — §5.** Источник границ территорий и классификатор кодов | открытые данные с фиксацией лицензии | до фазы 1 |
| Q7 | **Ответ владельца 24.09.2026 — §5.** Политика использования облачного ИИ | сменяемый провайдер, по умолчанию on-prem-совместимо | до фазы 1 |
| Q8 | Нужны ли мобильные нативные приложения и оффлайн-полевой сбор | PWA; полевой сбор — фаза 3+ | до фазы 4 |
| Q9 | **Ответ владельца 24.09.2026 — §5.** Интеграции с существующими системами (1С, HR, почта, AD) | SMTP/IMAP, LDAP, OIDC; остальное — по запросу | до фазы 5 |
| Q10 | **Ответ владельца 24.09.2026 — §5.** Масштаб и инфраструктура (число пользователей, данных, серверов) | S1 | до фазы 0 (влияет на compose-профиль) |
| Q11 | **Ответ владельца 24.09.2026 — §5.** Требования к хранению сообщений и записей (сроки, аудит) | бессрочно, политика в админке | до фазы 4 |
| Q12 | Межведомственный обмен документами | точка расширения | по требованию |
| Q13 | **Ответ владельца 24.09.2026 — §5.** Название продукта для пользователей и брендирование | «kchs» до уточнения; брендирование в админке | до релиза фазы 0 |

## 3. Открытые технические решения (исполнитель решает, фиксирует ADR)

- Точный формат состояния вкладок и стратегия keep-alive тяжёлых панелей.
- Библиотека drag-and-drop и виртуализации в списках (рекомендованы `@dnd-kit`, TanStack Virtual).
- Хранение результатов профилирования столбцов (таблица vs jsonb в датасете).
- Способ материализации `event_instances` календаря.
- Диаризация спикеров (pyannote — лицензия/ресурсы) — включать ли.
- Кэш тайлов: только Redis или диск/nginx с первого дня.
- Генерация OpenAPI-клиента: `openapi-fetch` vs `orval`.

## 4. Новые вопросы (заполняет исполнитель по ходу работы)

| # | Вопрос | Принятое рабочее допущение | Когда нужен ответ |
|---|---|---|---|
| N1 | ~~Среда разработки: git недоступен (не приняты соглашения Xcode Command Line Tools).~~ **Снят 18.09.2026**: история ведётся; фаза 0 — исходный коммит в `main` и ветка `phase/0-closeout`. | — | — |
| N2 | **Решение исполнителя 24.09.2026 — §5.** Учётные данные демонстрационной установки: администратор `admin` / `Kchs!Start-2026-7q`, сотрудники `user001…user060` / `Kchs!Work-2026-3v`. | Пароли задаются переменными `SEED_ADMIN_PASSWORD`, `SEED_USER_PASSWORD`; для боевой установки обязательна смена при первом входе. | до пилота |
| N3 | **Ответ владельца 24.09.2026 — §5.** Требуется ли принудительная MFA для ролей `system_admin` и `security_auditor` с первого дня? | MFA доступна всем, принудительность отключена; включается политикой `security.requireMfaRoles` — механизм реализован (ADR-0040), администратор отмечает роли в консоли «Безопасность». Открыто только значение по умолчанию. **Сделано:** `kchs init` делает второй фактор обязательным для «Администратора системы» и «Аудитора безопасности», демо-профиль это умолчание снимает (ADR-0139). | до релиза фазы 0 |
| N4 | **Ответ владельца 24.09.2026 — §5.** Производственный календарь РТ: какие дни `kchs init` должен заносить сам? | Заносятся праздники с постоянной датой по Трудовому кодексу РТ (1 января, 8 марта, 21–24 марта, 1 и 9 мая, 27 июня, 9 сентября, 6 ноября) на текущий и следующий год; Иди Рамазон, Иди Қурбон и переносы выходных объявляются постановлением Правительства — их вносит администратор, когда появится раздел календаря в консоли (ADR-0044). Открыто: нужен ли источник для автоматической загрузки или утверждённый список на годы вперёд. | до пилота |
| N5 | **Ответ владельца 24.09.2026 — §5.** К фазе 1 (Q6): источник территорий и классификатор кодов. P1-E07 S03 нужны территории для группировки и политик строк «только своя область». | Фаза 1 обходится без геометрий: справочник территорий из seed — 5 единиц первого уровня с кодами ISO 3166-2:TJ (TJ-DU Душанбе, TJ-SU Согдийская, TJ-KT Хатлонская, TJ-GB ГБАО, TJ-RA РРП) и районы демо-данных со своими устойчивыми кодами; при загрузке реальных данных коды сопоставляются справочником. Геометрии — к фазе 2 из открытых данных (geoBoundaries, CC BY 4.0) с лицензией в `seeds/README`, пока владелец не укажет официальный источник. | до загрузки реальных данных (пилот фазы 1); геометрии — до фазы 2 |
| N6 | **Ответ владельца 24.09.2026 — §5.** К фазе 1 (Q7): можно ли отправлять данные во внешний (облачный) ИИ? P1-E09 S03 «Спросить данные» строит запрос по вопросу на русском. | ИИ по умолчанию выключен (`AI_PROVIDER` пуст — функция скрыта, 13-search-knowledge-ai.md). Администратор включает облачного провайдера или on-prem (OpenAI-совместимый эндпоинт). Во внешний провайдер уходят схема датасета, профиль столбцов и агрегаты, не строки; каждый вызов — в аудит; запрос показывается пользователю и исполняется компилятором `packages/query` с политиками строк. | до включения на пилоте |
| N7 | **Ответ владельца 24.09.2026 — §5.** К фазе 2: источник численности населения по административным единицам — хороплет «на 1 000 жителей» (P2-E04 S04, ADR-0077) и показатели паспорта территории. | Население — атрибут `population` единицы справочника территорий (столбец `population` системного датасета «Территории»); в демо-данных — приблизительные значения сида. Реальные значения загружаются импортом справочника; единица без населения в хороплете получает пустое значение, а не ошибку. Открыто: официальный источник (Агентство по статистике при Президенте РТ?) и год, на который берутся данные. | до загрузки реальных данных |
| N8 | **Ответ владельца 24.09.2026 — §5.** К фазе 3: семантика согласования в маршрутах (ADR-0079). | «Отклонить» завершает параллельный шаг сразу, «Замечания» ждут ответов всех назначенных; при повторном согласовании «только отклонивших» (`rejecters_only`) заново согласуют все, кто не одобрил, включая не успевших ответить. Открыто: подтвердить или задать другую практику канцелярии. | до приёмки фазы 3 |
| N9 | **Ответ владельца 24.09.2026 — §5.** К фазе 3: срок шага последовательного согласования (ADR-0079). | Срок общий на шаг: очередь согласующих делит один срок от активации шага. Открыто: нужен ли каждому согласующему свой срок от момента его очереди. | до приёмки фазы 3 |
| N10 | **Ответ владельца 24.09.2026 — §5.** К фазе 3 (08-documents.md §13): применим ли гриф «Секретно» в системе? Сведения, составляющие государственную тайну, обычно обрабатываются только в аттестованных системах. | Гриф `secret` реализован наравне с остальными (ADR-0080), но ни у одного стартового типа, кроме «Донесения», он не разрешён; допуск `secret` администратор выдаёт вручную. Открыто: убрать `secret` из допустимых значений или оставить для служебных пометок, не являющихся гостайной. **Сделано:** гриф снят, прежние значения — «Конфиденциально» (ADR-0142). | до пилота документооборота |
| N11 | **Ответ владельца 24.09.2026 — §5.** К фазе 3: допуск сотрудников по умолчанию и порог обезличивания уведомлений. | Допуск по умолчанию — «Для служебного пользования» (`internal`): все сотрудники видят документы ДСП, если им дан доступ; «Конфиденциально» — только с выданным администратором допуском. Уведомления, письма, Telegram и Входящие скрывают тему документа начиная с «Конфиденциально»; ДСП — с темой. | до пилота документооборота |
| N12 | **Ответ владельца 24.09.2026 — §5.** К фазе 3: официальные форматы регистрационных номеров и состав журналов для органов РТ. | Стартовые журналы и шаблоны номеров (`ВХ-0001/26`, `001-ПР/26` …) — редактируемый сид; делопроизводитель меняет префикс, шаблон и сброс счётчика в справочнике журналов. Открыто: нужен ли индекс подразделения (`{unit.code}`) и номенклатурный индекс дела в номере по умолчанию. | до пилота документооборота |
| N13 | **Решение исполнителя 24.09.2026 — §5.** К фазе 3 (календарь, ADR-0081): какие календари показывать по умолчанию. Сотрудник видит календари всех пространств, где он участник; администратор системы участвует во всех пространствах подразделений — у него отмечено больше двадцати календарей. | Показываются личный календарь, календари пространств пользователя, проекты и подписки; ресурсы — только по отметке. Пользователь снимает отметки в левой колонке (запоминаются). Открыто: нужен ли набор по умолчанию «только своё подразделение» для руководителей и администраторов. | до пилота фазы 3 |
| N14 | **Решение исполнителя 24.09.2026 — §5.** К фазе 3 (маршруты, ADR-0083): состав стартовых маршрутов. Исходящее письмо — «юрист и профильный отдел» выбирает инициатор (ролей «юрист пространства» в оргструктуре нет), затем руководитель руководителя подразделения автора, подпись подписанта из карточки с кодом, регистрация канцелярией; приказ — согласующие по выбору, подпись с кодом, номер сразу после подписи; служебная записка — подпись руководителя подразделения без кода. | Маршруты редактируемые (новая версия определения не трогает идущие); сид их не перезаписывает. Открыто: утвердить состав и сроки (3/2/2 рабочих дня), нужна ли подпись с кодом для приказов и записок, нужен ли юрист всегда (роль `legal` в пространстве). | до пилота документооборота |
| N15 | **Ответ владельца 24.09.2026 — §5.** К фазе 3: согласование конфиденциальных документов. Согласующий или подписант без допуска к грифу документа шаг не получает — маршрут не запускается («нет допуска: …»), на ходу шаг не активируется. | Альтернатива — показывать такому участнику только карточку без содержания — отклонена: решение вслепую. Открыто: допустимо ли, что руководитель без допуска не может подписать конфиденциальный документ (выдать допуск или назначить другого подписанта). | до пилота документооборота |
| N16 | **Ответ владельца 24.09.2026 — §5.** К фазе 3 (резолюции, ADR-0084): кто вносит резолюции за руководителя и кто контролёр по умолчанию. В оргструктуре нет роли «помощник руководителя». | Резолюцию от имени руководителя вносит делопроизводитель (право регистрации документа) или заместитель «от имени»; контролёр по умолчанию — контролёр документа, иначе зарегистрировавший его. Открыто: нужна ли роль «помощник руководителя» с правом вносить резолюции только своего руководителя и быть контролёром по умолчанию. | до пилота документооборота |
| N17 | **Ответ владельца 24.09.2026 — §5.** К фазе 3 (ознакомление, ADR-0084): кого знакомить с приказами по умолчанию и нужна ли юридически значимая отметка. | Приказы и распоряжения при регистрации — сотрудникам подразделения документа (правило типа можно изменить: подразделения, срок, код), срок — 3 рабочих дня; отметка — кнопкой, с кодом второго фактора — по настройке типа или запроса. Открыто: знакомить ли со всеми приказами всю организацию и требовать ли код для приказов по основной деятельности. | до пилота документооборота |
| N18 | **Ответ владельца 24.09.2026 — §5.** К фазе 3 (08-documents.md §5, ADR-0085): реквизиты и место штампа регистрации входящего. | Штамп — в правом нижнем углу первого листа копии PDF (ГОСТ Р 7.0.97, «отметка о поступлении»): название организации, «Вх. № …», дата регистрации; накладывается сам после регистрации, версия документа не меняется. Открыто: нужны ли время поступления, число листов, индекс подразделения, другое место (верхний правый угол) или штамп только на бумажном оригинале. | до пилота документооборота |
| N19 | **Ответ владельца 24.09.2026 — §5.** К фазе 3 (08-documents.md §13, ADR-0085): кто может скачать исходник файла с грифом «Конфиденциально» без водяного знака. | Только администратор системы в режиме администратора — с аудитом; все остальные, включая автора и делопроизводителя, скачивают копию с водяным знаком (имя, время). Открыто: нужно ли право «выдачи исходника» автору или канцелярии, и какой текст водяного знака принят (сейчас: гриф, ФИО, дата и время). | до пилота документооборота |
| N20 | **Ответ владельца 24.09.2026 — §5.** К фазе 3 (дела и архив, ADR-0086): номенклатура дел и сроки хранения. Нужна утверждённая номенклатура дел Комитета и перечень сроков хранения документов, по которым канцелярия заводит дела и составляет акты о выделении к уничтожению. | Номенклатуру ведёт канцелярия в системе вручную (индекс, заголовок, год, срок в годах или «постоянно», статья перечня текстом); срок считается с 1 января года, следующего за годом дела; акт составляет владелец «вести журналы» с текстовым основанием (протокол экспертной комиссии). Демо-сид заводит пример номенклатуры со сроками 3, 5 лет и «постоянно». Открыто: импорт утверждённой номенклатуры, согласование акта экспертной комиссией маршрутом, передача дел в государственный архив. | до пилота документооборота |
| N21 | **Решение исполнителя 24.09.2026 — §5.** К фазе 3 (ADR-0086): правила исполнения без резолюции. Исходящий исполняется отправкой; зарегистрированный документ без контроля («к сведению») делопроизводитель подшивает в дело сразу. | Первая отметка об отправке переводит исходящий в «Исполнен»; подшивка зарегистрированного без контроля — переход «не требует исполнения» (`registered → executed → filed`); документ на контроле ждёт исполнения. Закрытое дело можно открыть снова, пока оно не передано в архив. Открыто: подтвердить практику канцелярии; нужна ли отметка «В дело» руководителем (резолюцией) вместо решения делопроизводителя. | до пилота документооборота |
| N22 | **Ответ владельца 24.09.2026 — §5.** К фазе 3 (сценарий B, шаг 6; ADR-0084, ADR-0086): закрывать ли поручение по резолюции автоматически, когда ответ «в ответ на» входящий зарегистрирован и отправлен. | Нет: ответственный отчитывается сам (в отчёте — номер исходящего), автор резолюции принимает отчёт; входящее исполняется по закрытию последнего поручения. Открытые направления на резолюцию закрытого документа (исполнен, подшит, в архиве) снимаются сами. Открыто: нужен ли автоотчёт по отправке ответа и кто тогда принимает его — автор резолюции или контролёр. | до пилота документооборота |
| N23 | **Ответ владельца 24.09.2026 — §5.** К фазе 4 (ADR-0090): тихие часы и «не беспокоить» действуют только на уведомления чатов — статус присутствия. Распространять ли их на все категории (поручения, согласования, напоминания о встречах) и с каким исключением для срочного? | Тихие часы и «не беспокоить» из `/me/presence` глушат внешние каналы (Telegram, почта) для `chat.*`; значок в приложении остаётся всегда. Поля `quietHours`/`doNotDisturbUntil` в настройках уведомлений ядра пока не применяются. **Сделано:** тишина глушит Telegram, push и письма всех категорий через порт ядра; срочны алерты, эскалации, поручения P1, напоминания о встрече и правила с флагом `urgent` (ADR-0140). | до приёмки фазы 4 |
| N24 | **Решение исполнителя 24.09.2026 — §5.** К фазе 4 (ADR-0091): медиасервер в compose объявляет адрес контейнера (`use_external_ip: false`), и браузер на хосте с Docker Desktop (macOS) до потока не доходит — ICE обрывается через несколько секунд. | Решено при приёмке фазы: у службы `livekit` появился необязательный `LIVEKIT_NODE_IP` (флаг `--node-ip`). На стенде разработчика в него ставят адрес хоста в сети (не `127.0.0.1`: его не видит Egress из своего контейнера), и тогда работают и браузер на хосте, и запись. В установке переменная пуста — узел определяет адрес сам. Открыто: нужен ли отдельный внешний адрес (`use_external_ip`) для установок за NAT. | до пилота фазы 4 |
| N25 | **Решение исполнителя 24.09.2026 — §5.** К фазе 4 (ADR-0091): кого пускать в комнату ожидания. Сейчас ждут только гости по ссылке, приглашённые участники входят сразу по праву `join`. | Участник системы не ждёт: его право уже проверено, а организатора в комнате может не быть. Открыто: нужен ли режим «ждут все, кроме организатора» для больших совещаний. | до пилота фазы 4 |
| N26 | **Решение исполнителя 24.09.2026 — §5.** К фазе 4 (ADR-0091): срок гостевой ссылки по умолчанию — 4 часа (от 15 минут до суток), отзыва ссылки отдельной кнопкой нет: она обесценивается завершением встречи. | Достаточно для приглашения на встречу дня. Открыто: нужен ли журнал выданных ссылок и их досрочный отзыв (тогда ссылка станет записью в базе, а не подписанным токеном). | до пилота фазы 4 |
| N27 | **Ответ владельца 24.09.2026 — §5.** К фазе 4 (ADR-0092): какая модель распознавания речи ставится на сервер и хватает ли её для таджикского. Качество для таджикского — известный риск (Q9 ниже): у Whisper таджикский покрыт слабо. | Модель задаётся `ENGINE_TRANSCRIBE_MODEL` и по умолчанию пуста — расшифровка выключена, интерфейс честно говорит «модель не настроена», ручной режим (слушать запись) остаётся. Рекомендуемое начало — `large-v3` на процессоре с `int8` для русского и английского; для таджикского расшифровку лучше не включать, пока владелец не примет качество на своих записях. Открыто: какая модель и какое железо выделяются, включать ли таджикский вообще. | до пилота встреч |
| N28 | **Решение исполнителя 24.09.2026 — §5.** К фазе 4 (ADR-0092): нужна ли диаризация (кто говорит) в расшифровке и протоколе. Сейчас поле спикера пустое: composite-дорожка комнаты сводит всех в один канал, а отдельная модель (pyannote) требует своих весов, токена и времени, сопоставимого с расшифровкой. | Расшифровка идёт без спикеров; поле в контракте есть и заполнится, если появится запись по участникам (отдельная дорожка на человека) или отдельная модель. Черновик протокола сможет опираться на список участников встречи, а не на спикеров. Открыто: нужна ли диаризация для протоколов и приемлемо ли удвоение времени обработки. | до пилота протоколов |
| N29 | **Ответ владельца 24.09.2026 — §5.** К фазе 4 (ADR-0092): срок хранения записей встреч и кто может их удалять. Час записи комнаты 720p — примерно 0,5–1 ГБ; десяток совещаний в неделю заметно растит хранилище. | Записи хранятся бессрочно как обычные файлы реестра; удалить запись может владелец (организатор встречи) или администратор. Открыто: нужен ли автоматический срок (например, 6 месяцев) с предупреждением организатору и исключением для записей, попавших в протокол или дело. **Сделано (ADR-0138):** записи хранятся 6 месяцев (настройка «Встречи» в консоли, 0 — бессрочно); за неделю до удаления — предупреждение организатору, закрепление в карточке записи; связанные с протоколом или документом не удаляются. | до пилота встреч |
| N30 | **Ответ владельца 24.09.2026 — §5.** К фазе 4 (протокол, ADR-0093): кто ведёт протокол. Сейчас повестку и протокол правит любой участник встречи (уровень `comment`), подтверждает организатор. | Совместная правка — всем участникам: протокол пишут по ходу встречи, секретаря как роли в системе нет. Открыто: нужна ли роль «секретарь встречи» (право правки без права входа в комнату) и надо ли закрывать правку всем, кроме организатора и секретаря. **Сделано (ADR-0137):** правят организатор и секретарь (роль участника, назначает организатор в карточке встречи), остальные участники читают и обсуждают. | до пилота фазы 4 |
| N31 | **Решение исполнителя 24.09.2026 — §5.** К фазе 4 (ADR-0093): что можно править после подтверждения протокола. Сейчас текст закрыт: поручения уже ушли исполнителям. | Исправление — новым блоком с оговоркой или новой версией зарегистрированного документа. Открыто: нужна ли канцелярская практика «протокол с изменениями» (повторное подтверждение с версией и отметкой, кто и что изменил). | до пилота фазы 4 |
| N32 | **Ответ владельца 24.09.2026 — §5.** К фазе 4 (ADR-0093): каким типом документа регистрируется протокол и нужен ли ему файл. Сейчас человек выбирает тип (стартовый «Протокол»), текст протокола попадает в краткое содержание, файла версии нет — печатная форма протокола не сделана. | Документ заводится черновиком, дальше — обычный маршрут согласования и подписи. Открыто: нужна ли печатная форма протокола (PDF с повесткой, решениями и поручениями) как версия документа и обязательна ли она для подписи. **Сделано (ADR-0137):** печатная форма протокола (повестка, ход заседания, поручения, подписи председателя и секретаря) при регистрации становится вложением и первой версией документа. | до пилота фазы 4 |
| N33 | **Ответ владельца 24.09.2026 — §5.** К фазе 4 (ADR-0093): срок поручений из протокола, если он не назван на встрече. Сейчас подтвердить блок без срока нельзя. | Организатор проставляет срок руками; ИИ предлагает только названную дату. Открыто: нужен ли срок по умолчанию (например, 10 рабочих дней от подтверждения) и общий срок исполнения протокола. **Сделано (ADR-0137):** без названного срока поручение получает 10 рабочих дней по производственному календарю; организатор ставит свою дату в блоке или меняет срок поручения. | до пилота фазы 4 |
| N34 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (база знаний, ADR-0095): кто ведёт базу знаний и кто публикует. Сейчас страницу правит любой, у кого есть право `edit` в пространстве, а публикует, назначает владельца и срок пересмотра — уровень `manage` (у автора он есть: он владелец объекта). | Так база знаний живёт как вики: пишут многие, отвечает за опубликованный текст владелец страницы. Открыто: нужна ли роль «редактор базы знаний» на пространство и следует ли закрыть правку опубликованной страницы всем, кроме владельца. | до пилота фазы 5 |
| N35 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0095): срок пересмотра по умолчанию и что делать с просроченной страницей. Сейчас срок не назначается сам, а по его наступлении страница уходит в «на пересмотре» и владельцу открывается дело; из выдачи и печати она не исчезает. | Молчаливое устаревание хуже видимого: читатель видит отметку «на пересмотре», но текст остаётся доступен. Открыто: нужен ли срок по умолчанию (например, год от публикации) для регламентов и инструкций и нужно ли предупреждать читателя отдельно, если пересмотр просрочен больше чем на месяц. **Сделано:** регламенту и инструкции при публикации ставится год от публикации (дату можно поменять, но не убрать), опубликованным без срока его проставила миграция; при просрочке больше месяца читатель видит предупреждение (`knowledge.test.ts`). | до пилота фазы 5 |
| N36 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0095): что должно попадать в PDF страницы. Сейчас печатаются текст, таблицы и картинки; график, карта, таблица датасета, показатель и список задач заменяются строкой со ссылкой на объект — их содержимое строится в браузере, а страница печати документов работает с закрытой сетью. | Регламент печатается ради текста, а встроенные представления живут в системе. Открыто: нужен ли для страниц путь печати через страницу в Chromium (как у отчётов, ADR-0078), чтобы графики и карты попадали в PDF картинками. | до пилота фазы 5 |
| N46 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (DoD §3): с этой фазы заглушек таджикского недостаточно, а сейчас `tg` покрыт на 6 % (324 ключа из 5310). Перевод интерфейса — работа не программиста: нужен глоссарий терминов и носитель языка. | Допущение по умолчанию: машинный перевод по глоссарию с вычиткой носителем, ключи без перевода падают на `ru` (механизм уже есть). Открыто: кто вычитывает, нужен ли таджикский в первом пилоте и какие разделы важнее (вход, Входящие, документы). | до приёмки фазы 5 |
| N43 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0098): что считать признаком отключённой записи в каталоге заказчика и по какому атрибуту сопоставлять сотрудника между прогонами. По умолчанию — `userAccountControl` (бит 2) и `objectGUID` у Active Directory; у OpenLDAP признак отключения бывает `nsAccountLock`, `pwdAccountLockedTime` или отсутствует вовсе, а устойчивый идентификатор — `entryUUID`. | Оба атрибута настраиваются в разделе «Каталог» и проверяются кнопкой «Проверить соединение» и предпросмотром; умолчания взяты по Active Directory. Открыто: какой каталог в организации, как в нём помечается уволенный и есть ли у записей устойчивый идентификатор — без него сотрудник опознаётся по логину и переименование в каталоге заведёт вторую учётную запись. | до подключения каталога на пилоте |
| N44 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0098): распространяется ли требование второго фактора платформы (`security.requireMfaRoles`) на вход через корпоративный IdP. Сейчас — да: сотруднику с такой ролью платформа всё равно предложит подключить код приложения или ключ входа, даже если провайдер уже спросил второй фактор. | Гарантия организации не зависит от настройки провайдера; подключить фактор можно ключом входа, без приложения-аутентификатора. Открыто: принять это или доверять провайдеру — тогда нужен список значений `acr`/`amr`, при которых вход считается подтверждённым двумя факторами, и договорённость с администратором IdP о том, что они не подделываются. | до включения единого входа на пилоте |
| N45 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0098): кто и как восстанавливает доступ сотруднику каталога или IdP, потерявшему устройство. У записи каталога локального пароля нет, ссылка восстановления ведёт к смене локального пароля, а ключ входа отзывается только своим владельцем в профиле. | Пароль такому сотруднику меняет администратор каталога или IdP — вне платформы; администратор системы может заблокировать учётную запись и завершить её сессии. Отозвать чужой ключ входа сейчас нельзя ни администратору, ни владельцу утерянного устройства — только с другого устройства. Открыто: нужна ли администратору кнопка «отозвать все ключи входа сотрудника» (с аудитом) и должно ли восстановление по ссылке переводить учётную запись каталога на локальный пароль. **Сделано:** «Пользователи» → меню сотрудника → «Ключи входа»: список ключей и «Отозвать все» с записью `user.passkeys_revoked` в аудит; «Сбросить MFA» ключи не трогает, руководства это объясняют. | до пилота единого входа |
| N40 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0097): кому разрешать выпуск токенов публичного API. Способность `api_tokens.create` сейчас только у администратора системы, поэтому аналитик или делопроизводитель токен себе не выпустит — придётся просить администратора. | Начали со строгого: токен живёт дольше сессии, вторым фактором не подтверждается и легко утекает в чужой журнал. Открыто: выдавать ли способность ролям `data_steward` и `gis_admin` (интеграции с внешними источниками — их работа) и нужно ли ограничение «не больше N действующих токенов на человека». | до пилота интеграций |
| N41 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0097): какие маршруты открывать токенам. Сейчас область выводится из тега маршрута, а личный кабинет, вход, администрирование и ИИ закрыты при любых областях. | Закрыто по умолчанию: новый модуль сам решает, открывать ли своё API. Открыто: нужен ли интеграциям доступ к администрированию (заведение пользователей из кадровой системы, синхронизация оргструктуры) — тогда понадобится область `write:org` на маршрутах `/admin/*` и отдельная отметка в аудите. | до пилота интеграций |
| N42 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0097): состав пакета конфигурации. Переносятся интеграции, вебхуки и маршруты; типы документов перенести нельзя — карточка типа ссылается на журнал регистрации по UUID, а стабильного ключа у журналов нет (`journals.name` меняется). | Раздел «типы документов» объявлен и в предпросмотре честно помечается «не поддержана». Открыто: добавить ли журналам стабильный ключ (миграция и правка контракта `08-documents.md`) — после этого в пакет поедут типы документов, шаблоны, справочники, показатели, дашборды и карты по той же схеме. | до переноса настройки между контурами |
| N37 | **Ответ владельца 24.09.2026 — §5. Сделано: ADR-0130.** К фазе 5 (правила автоматизации, ADR-0096): кто такие служебные учётные записи правил. Правило работает от имени `run_as` — действующего пользователя без прав администратора системы; отдельного вида «служебная учётная запись» в оргструктуре нет. | Администратор заводит обычного пользователя (например, «Автоматизация канцелярии») с нужными ролями и включает его в нужные пространства; правило не может больше, чем он. Открыто: нужен ли отдельный вид учётной записи (без входа по паролю, без почты и Telegram, видимый в справочнике отдельно) и запрет назначать `run_as` живого сотрудника. | до пилота автоматизации |
| N38 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0096): кому можно писать письма и вызывать внешние адреса из правил. Действие `send_email` принимает и выражения назначений, и явные адреса; `webhook` — любой `https`-адрес. | Правила ведёт владелец способности `automation.manage` (по умолчанию — администратор системы), каждый вызов и письмо попадают в журнал запусков. Открыто: нужен ли белый список доменов для исходящих вызовов и запрет писем на адреса вне организации. **Сделано:** списки «Почтовые домены» и «Домены вебхуков» в политике безопасности; сотрудникам из справочника письма идут всегда, явные адреса и вебхуки — только по списку; проверка при сохранении, включении и запуске (ADR-0141). | до пилота автоматизации |
| N39 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0096): что делать с правилами при переносе конфигурации между контурами и при удалении служебного пользователя. Ключ правила (`key`) стабилен, но `run_as`, пространства и показатели — идентификаторы установки. | Пока: правило переносится с ключом, `run_as` и ссылки на объекты администратор задаёт заново в целевом контуре; при удалении пользователя `run_as` обнуляется и запуски падают с понятной ошибкой владельцу правила. Открыто: нужен ли экспорт правил в пакет конфигурации (14-automation-integrations.md §6) с сопоставлением служебных пользователей и автоматическое выключение правил с недоступным `run_as`. | до переноса конфигурации между контурами |
| N47 | **Ответ владельца 24.09.2026 — §5. Сделано (ADR-0130): форма пишет строки только от служебной учётной записи.** К фазе 5 (ADR-0103): от чьего имени форма пишет строки датасета. Сейчас — служебный пользователь формы (`run_as`, по умолчанию её создатель): назначенному достаточно видеть форму, права на датасет ему не выдаются. | Альтернатива — выдавать каждому сдающему подразделению право правки датасета; она шире, чем нужно, и открывает правку чужих строк. Открыто: подтвердить схему со служебным пользователем и решить, кто становится `run_as` при уходе сотрудника (сейчас запись просто перестаёт работать с понятной ошибкой). | до пилота сбора сводок |
| N48 | **Сделано 24.09.2026 (ADR-0129):** ответственный за сдачу в назначении подразделения получает дело, напоминания и эскалацию. **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0103): кому адресовать «Сдать сводку», если у назначенного подразделения нет руководителя. Сейчас дело уходит его сотрудникам (не больше десяти). | Рабочее допущение: оргструктура заполняется до начала сбора, и у сдающих подразделений руководитель есть. Открыто: не лучше ли указывать ответственного за сдачу прямо в назначении формы. | до пилота сбора сводок |
| N49 | **Сделано 24.09.2026 (ADR-0129):** табличные формы — строки за период одной сдачей, итоги из строк, пустая сдача «записей не было». **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0103): одна отправка — одна строка датасета (сводка за период). Табличные формы «много строк за период» (список происшествий за сутки) не реализованы. | Допущение: первые формы — числовые сводки. Открыто: нужны ли табличные формы в первом контуре и как в них выглядит контроль сдачи (ячейка «сдано» на набор строк). | до пилота сбора сводок |
| N50 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0104): точность расписания алертов — пять минут (тик единого планировщика). Алерт с cron «каждую минуту» проверяется раз в пять минут. | Допущение: алерты на показатели — часовые и суточные, минутная точность им не нужна. Открыто: есть ли сценарии, где нужна проверка чаще. | до нагрузочных тестов S2 |
| N51 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0104): показатель алерта считается под правами владельца алерта. Получатель может увидеть в сообщении число, посчитанное по строкам, которых сам не видит. | Допущение: алерты заводят на показатели, открытые всей группе получателей. Открыто: не нужно ли запрещать каналы доставки, если получатель не видит показатель, или считать значение для каждого получателя отдельно. | до пилота алертов |
| N52 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5: правила качества `in_set` и бейдж качества в каталоге (ADR-0101) собирают условие `= any(${массив})` в шаблоне `sql`, где drizzle разворачивает массив в список параметров — запрос падает. Та же ошибка встретилась в моём коде и исправлена переходом на `inArray`. | Своего кода я не касался в чужой истории. Открыто: подтвердить ошибку и исправить `quality-service.ts` (`in_set`, `statuses`) тем же способом; интеграционного теста на `in_set` сейчас нет. | до приёмки фазы 5 |
| N65 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0113): нужны ли ответы из почты в обсуждение документа. `14-automation-integrations.md` §5 помечает это «опц.»; сейчас почта работает в одну сторону — письмо становится черновиком входящего. | Обратный путь требует адреса вида `doc+<id>@…` в `Reply-To` исходящих писем, разбора этого адреса на входе и правила «кто может писать в обсуждение письмом» (адрес отправителя — слабое удостоверение). Открыто: нужен ли такой путь на пилоте и допустимо ли считать совпадение адреса достаточным подтверждением личности. | до пилота канцелярии |
| N66 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0113): что делать с письмом от неизвестного корреспондента. Сейчас черновик заводится без корреспондента, а очередь показывает «не найден по адресу — заведите корреспондента»; заводит его делопроизводитель вручную при регистрации. | Автоматическое заведение корреспондента по адресу засоряло бы справочник (рассылки, личные ящики, опечатки в домене). Открыто: заводить ли корреспондента автоматически для адресов из белого списка доменов ведомств и нужно ли сопоставление по домену, а не по полному адресу (`@mvd.example.tj` → «МВД»). | до пилота канцелярии |
| N67 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0113): сколько хранить записи очереди «Из почты» и тексты писем. Сейчас записи живут вечно: по ним видно, что пришло и почему не стало документом. | Текст письма ограничен 100 000 символов, вложения лежат обычными файлами документа и живут по его правилам. Открыто: нужна ли чистка отклонённых и неразобранных писем по сроку (например, 180 дней) и нужно ли прятать текст письма от тех, кто не видит заведённый из него документ. | до пилота канцелярии |
| N68 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0112): держать ли сервер документов ONLYOFFICE в контуре организации и в какой редакции. Реализация рассчитана на самостоятельное развёртывание (профиль `office`); Community Edition ограничивает число одновременных подключений. | Пока — Community Edition в контуре организации, без выхода в интернет. Открыто: ожидаемое число одновременно редактирующих и нужна ли лицензия Enterprise; облачная версия ONLYOFFICE не рассматривается — содержимое документов не должно покидать контур. | до пилота документооборота |
| N69 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0112): что делать с файлами под грифом от «конфиденциально». Сейчас они во внешнем редакторе не открываются вовсе — исходник наружу не уходит (ADR-0085). | Правка такого файла идёт прежним путём: скачать, поправить, загрузить новую версию. Открыто: допустимо ли редактировать документы с грифом «для служебного пользования» и выше на сервере документов, развёрнутом в том же защищённом контуре, и нужен ли для этого отдельный экземпляр сервера. | до пилота документооборота |
| N70 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0112): нужна ли блокировка файла на время совместной правки. Сейчас блокировка (`locked_by`) ставится вручную и переводит чужой редактор в режим просмотра, но сам редактор её не ставит: несколько человек правят один документ одновременно — в этом смысл совместного редактирования. | Конфликт версий не теряет правку: она ложится следующей версией с отметкой, поверх какой сохранена. Открыто: должна ли карточка документа показывать «файл сейчас правят» (сессия редактора видна в базе) и нужно ли запрещать загрузку новой версии обычным путём, пока редактор открыт. | до пилота документооборота |
| N53 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (пайплайны, ADR-0106): что считать геокодированием. Сейчас шаг `geocode` сопоставляет поле со справочником территорий по коду или точному названию и добавляет идентификатор территории и точку её центра; разбора адресов («ул. Рудаки, 12») нет. | Для сводок по территориям этого достаточно, а нечёткое сопоставление без внятного порога даёт молчаливо неверные привязки. Открыто: нужен ли разбор адресов, по какому справочнику адресов (его в стране может не быть) и что делать со строками, которые не сопоставились, — отбрасывать, помечать или ронять прогон. | до пилота с адресными данными |
| N54 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0106): нужен ли свой SQL в середине цепочки. Сейчас `custom_sql` — шаг-источник: только первый и единственный, дальше преобразования делает второй пайплайн поверх результата. | Так сырой SQL остаётся на единственном пути (разбор `libpg-query` + роль `kchs_query`), а компиляция цепочки — синхронной. Открыто: достаточно ли этого аналитикам или стоит принять сырой SQL промежуточным источником `QuerySpec` (тогда компилятор запросов станет асинхронным, а проверку переписанного запроса придётся делать дважды). | по отзывам аналитиков на пилоте |
| N55 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (внешние БД, ADR-0107): чем ограничивать запрос во внешней базе. Запрос пользователя выполняется там целиком под учётной записью подключения; платформа проверяет только имена (схема, таблица, поле-курсора) и подставляет значение курсора параметром. | Ограничивать должна учётная запись подключения — только чтение и только нужные таблицы; об этом сказано в подсказке экрана. Открыто: нужен ли платформе свой разбор запроса к чужой СУБД (у каждой свой диалект) и потолок времени/строк на стороне источника, помимо наших тайм-аутов. | до подключения боевой базы заказчика |
| N56 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0107): какие ещё СУБД нужны. Реализованы PostgreSQL и MySQL/MariaDB; MS SQL и ClickHouse из `06-analytics-engine.md` §2 — нет. | Вид интеграции добавляется перечислением и одним драйвером, поэтому расширение ничего не ломает. Открыто: какие СУБД реально стоят у заказчика (1С обычно на MS SQL или PostgreSQL) и нужна ли ClickHouse до колоночного tier. | до пилота интеграций |
| N57 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (внешние ГИС-службы, ADR-0108): откуда администратор берёт названия слоёв и наборов матриц. Сейчас `LAYERS`, `LAYER`, `TILEMATRIXSET` и `typeName` вводятся руками по документации службы. | Ошибка видна сразу: «Проверить соединение» запрашивает тайл или первую страницу объектов. Открыто: делать ли разбор `GetCapabilities` с подсказкой списка (у WMS, WMTS и WFS это три разных XML-схемы и три разных набора версий). | до подключения гос. служб на пилоте |
| N58 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0108): кто отвечает за то, что внешняя служба показывается всем сотрудникам. Слой-ссылка — объект установки с `everyone:view`; политик строк у чужих данных нет и быть не может. | Заводит и выключает службы владелец способности `gis.basemaps.manage` — решение принимает администратор ГИС, а не автор карты. Открыто: нужны ли службы с ограниченной видимостью (по пространствам или ролям) и надо ли отмечать их гриф, если служба отдаёт чувствительные данные. | до подключения служб с ограничением доступа |
| N59 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0110): вес отложенной сборки deck.gl. `MVTLayer` живёт в бочке `@deck.gl/geo-layers`, которая тянет `h3-js` и `@loaders.gl/wms` (они не используются), а подпутей пакет не публикует — отложенный кусок 507 КБ gz вместо примерно 150 КБ. | Оставлено как есть: кусок грузится только на карте со слоем сверх порога, основная сборка не изменилась. Варианты на будущее: свой тайловый слой на `@deck.gl/core` + `@deck.gl/layers` + `@loaders.gl/mvt` или ожидание подпутей в deck.gl. | до нагрузочных тестов S2 |
| N60 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0110): значение порога deck.gl по умолчанию — 50 000 объектов слоя. | Порог настраивается администратором (раздел «Базовые карты»); 50 000 выбрано по тому, где MapLibre на тайлах начинает заметно проседать на обычной рабочей машине. Открыто: подтвердить значение на данных установки. | до пилота фазы 5 |
| N61 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0110): в deck.gl уходят только простые роли стиля (заливка, линия, точка-кружок). Слой с кластерами, подписями, тепловой картой или точками-значками остаётся в MapLibre независимо от размера. | Так стиль остаётся одним и тем же, а легенда не расходится с картой. Открыто: нужны ли подписи и кластеры в ускоренной отрисовке — это отдельные слои deck.gl и своя ветка легенды. | до приёмки фазы 5 |
| N62 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0109): порог колоночного tier — 1 млн строк, и копия собирается сама при новой версии данных крупного датасета. | Порог и выключатель — в администрировании; сборка 5 млн строк занимает около 30 с и 83 МБ в хранилище. Открыто: подтвердить порог и нужно ли ограничить время суток для пересборки копий больших датасетов. | до пилота фазы 5 |
| N63 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0109): денежные и точные числа колоночная копия хранит как `DECIMAL(38,12)`, а сам Postgres допускает `numeric` без ограничений. Значение с 27 и более цифрами до запятой в копию не поместится. | Сборка такой копии завершится ошибкой, датасет останется на Postgres-пути — данные не портятся. Открыто: нужны ли такие величины в датасетах установки. | до загрузки реальных данных |
| N64 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0109): колоночная копия не хранит геометрию, поэтому пространственные запросы и карты всегда считает Postgres. | Для карт это и не нужно: тайлы отдаёт PostGIS. Открыто: нужен ли DuckDB spatial для тяжёлого пространственного анализа (07-gis-engine.md §15, сценарий S2). | до нагрузочных тестов S2 |
| N89 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (ADR-0127, сценарий C): последний шаг сценария — «страница в базе знаний с историей версий». Отчёт теперь становится исходящим документом (согласование, подпись, регистрация, рассылка), но страницы знаний из отчёта пока не рождается. | Сделать это просто: страница с текстовым блоком и блоком-файлом того же файла. Открыто: нужна ли такая публикация вообще (у документа есть своя карточка и дело) и кто решает, в каком пространстве и разделе знаний она появляется. | до приёмки фазы 5 |
| N77 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (P5-E07, ADR-0097): кто в организации получает способность `automation.manage`. Она даёт завести исходящий вебхук с подпиской `*` — и тогда каждое событие установки (заголовки документов, названия объектов, идентификаторы) уходит на чужой адрес. Фильтр по пространствам и типам задаёт сам подписчик, а не права: доступ проверяется только для событий с `visibility.principals`. | Пока способность выдана администратору системы, то есть тому, кто и так видит всё. Открыто: подтвердить, что `automation.manage` остаётся административной; если её будут выдавать руководителям подразделений, подписке нужна проверка доступа к каждому событию (а не только к адресным), и это заметно дороже. | до выдачи способности за пределы администраторов |
| N78 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0107): «Проверить соединение» у внешней базы возвращает сообщение драйвера как есть, и по нему отличаются «порт закрыт», «тайм-аут», «не та СУБД» и «неверный пароль». Частные сети (10/8, 172.16/12, 192.168/16) для источников открыты намеренно — база закрытого контура обычно в них. Вместе это даёт владельцу `automation.manage` точный признак живого порта во внутренней сети. | Пока оставлено: администратору, который настраивает подключение, текст ошибки нужен, а способность и так административная (см. N77). Открыто: не заменить ли сообщение на обобщённое («узел не отвечает» / «база отказала в доступе») с полным текстом только в журнале сервера. | до выдачи способности за пределы администраторов |
| N79 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0107): тайм-аут запроса стоит только на потоковом чтении из PostgreSQL. `rows`, `columns` и `tables` у PostgreSQL и весь драйвер MySQL ограничены лишь тайм-аутом соединения, поэтому медленный или недобросовестный источник держит обработчик до разрыва сокета. Это видно на предпросмотре выборки и на списке таблиц. | Пока: источники подключает администратор, число одновременных проверок ограничено лимитом маршрутов (20–30 в минуту). Открыто: ставить ли `statement_timeout` на все запросы PostgreSQL и `MAX_EXECUTION_TIME` на SELECT MySQL — правка небольшая, но меняет поведение уже подключённых источников с долгими выборками. **Сделано:** потолок у каждого запроса — `statement_timeout` сессии PostgreSQL, тайм-аут клиента и `max_execution_time` MySQL; предпросмотр и проверка — 30 с (`EXTERNAL_DB_TIMEOUT_MS`), загрузка в задании — 10 минут; истечение — понятная ошибка 504. | до подключения боевой базы заказчика |
| N80 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0121): пять уязвимостей уровня high в зависимостях разобраны и внесены в `auditConfig.ignoreGhsas`, но не исправлены: `drizzle-orm < 0.45.2` (инъекция через `sql.identifier()` — мы его не вызываем), `nodemailer <= 9.0.0` и `< 9.1.0` (у нас 7.x), `image-size` из deck.gl (код в сборку не попадает). | Пока: порог включён, список разобран построчно. Открыто: обновлять ли `nodemailer` 7 → 10 и `drizzle-orm` 0.44 → 0.45 отдельной историей (первое — два мажорных шага у отправки почты, второе — минорный у ORM вместе с `drizzle-kit` и миграциями) и как часто пересматривать список. | до релиза фазы 5 |
| N81 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0121, `17-security.md` §4): «ротация с перешифрованием» мастер-ключа `KCHS_MASTER_KEY` описана в проекте, но средства перешифрования в установке нет. Смена ключа делает нечитаемыми секреты TOTP, пароль каталога и секрет IdP, секреты интеграций и подписи вебхуков, ключи ГИС-служб и подложек, адреса подписок на календари. | Пока: порядок ручной ротации расписан в `infra/runbooks/secret-leak.md` §3 — что придётся ввести заново и что будет с людьми (подключение второго фактора заново). Открыто: нужна ли команда `kchs secrets rotate --old-key` (читает старым ключом, пишет новым, в одной транзакции по каждой таблице) и к какому сроку. **Сделано:** `kchs secrets rotate` с прежним ключом в `KCHS_MASTER_KEY_PREVIOUS` — смена без простоя и без повторного ввода секретов (ADR-0143, runbook `secret-leak.md` §3). | до первой ротации ключа на боевой установке |
| N82 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (ADR-0098): строки незавершённых входов через IdP (`sso_auth_requests`) и вызовов ключей входа (`webauthn_challenges`) не удаляются. Одноразовость обеспечена отметкой `used_at` и удалением при проверке, но брошенные попытки (человек закрыл вкладку) остаются навсегда — вместе с зашифрованным проверочным кодом PKCE и `nonce`. | Пока: строки просрочены и в дело не идут, объём мал (одна попытка входа — одна строка). Открыто: добавить ли их в ежедневное задание обслуживания рядом с истёкшими сессиями и ссылками (`15-admin-operations.md` §6) — правка на несколько строк, но это чужая история обслуживания. **Сделано:** ежедневное задание `identity.prune` (03:37) удаляет истёкшие и отозванные сессии и ссылки сброса пароля через неделю, брошенные вызовы второго фактора и ключей входа и незавершённые входы через IdP — через сутки. | до релиза фазы 5 |
| N71 | **Отложено до S2 — §5.** К фазе 5 (ADR-0118): на какой кластер рассчитывать чарт S2. Сейчас предполагается, что администратор сам ставит контроллер Ingress, cert-manager, KEDA, Prometheus и оператор Postgres, а чарт знает о них только адреса и имена классов. | Так чарт не навязывает состав платформы и работает в закрытом контуре. Открыто: какой дистрибутив Kubernetes и какие из этих компонентов уже есть у заказчика — от этого зависит, нужны ли в чарте готовые наборы значений под конкретную платформу и не проще ли заменить KEDA на то, что уже стоит. | до проектирования стенда S2 |
| N72 | **Отложено до S2 — §5.** К фазе 5 (ADR-0118): порог масштабирования воркера — 50 ожидающих заданий на реплику, движка — 5. | Числа взяты из соотношения «дешёвые задания ядра (уведомления, индекс) против минутных заданий движка (импорт, отчёт, расшифровка)», а не из замера. Открыто: подтвердить их на стенде S2 и решить, не нужны ли разные пороги по очередям (импорт и печать отчётов ведут себя по-разному). | до нагрузочных тестов S2 |
| N73 | **Отложено до S2 — §5.** К фазе 5 (ADR-0118): встроенные Postgres, Redis, MinIO и Meilisearch в чарте — один экземпляр без репликации, они объявлены режимом стенда. | Промышленная установка выключает их и получает адреса управляемых служб. Открыто: не стоит ли запретить их при `NODE_ENV=production` совсем, чтобы «стенд» не переехал в эксплуатацию незаметно, и достаточно ли для этого предупреждения в NOTES. | до первой установки S2 |
| N74 | **Отложено до S2 — §5.** К фазе 5 (ADR-0118): хранилище публикуется отдельным именем, и браузер ходит в него напрямую по подписанным ссылкам. В кластере это значит второй Ingress (или адрес облачного S3) и второй сертификат. | Иначе подпись SigV4 не сойдётся по заголовку `Host` (ADR-0044), а весь файловый трафик пошёл бы через api. Открыто: устраивает ли службу эксплуатации второе публичное имя и не нужен ли путь «всё через один домен» (S3 за тем же Ingress по префиксу) для контуров с одним сертификатом. | до проектирования стенда S2 |
| N75 | **Отложено до S2 — §5.** К фазе 5 (ADR-0120): чарт проверяется статически (`helm lint`, `helm template`, `kubeconform` на трёх наборах значений); установка в настоящем кластере откладывается до стенда S2. | У исполнителя кластера нет, а поднимать его рядом с общим стендом разработки рискованно. Открыто: нужно ли ночное задание с `kind` рядом со `stack.yml` (оно тянет сборку четырёх образов, включая движок на 4 ГБ) или достаточно проверки при установке стенда S2. | до приёмки фазы 5 |
| N76 | **Отложено до S2 — §5.** К фазе 5 (ADR-0119): профили S2 берут масштаб «1000 одновременных сотрудников» как утроенный предел S1 и 200 тайлов в секунду как порог из `15-admin-operations.md` §8. | Это ориентиры проектной документации, а не ожидания заказчика. Открыто: сколько сотрудников и какая доля работы с картами ожидаются в первом контуре S2 — от этого зависят и цифры профилей, и размер кластера. | до заказа оборудования S2 |
| N83 | К фазе 5 (P5-E07): письмо восстановления доступа ведёт на `/reset-password?token=…`, но такого пути в веб-приложении нет — токен теряется, открывается обычный экран входа. Серверный маршрут подтверждения пароля при этом работает. | В руководстве пользователя честно написано: забыли пароль — обратитесь к администратору, он выдаст временный. Открыто: доделать экран установки пароля по ссылке или убрать ссылку «Не удаётся войти?» и оставить только администратора. **Закрыто:** экран `/reset-password` добавлен, ссылка из письма работает. | до пилота |
| N84 | К фазе 5 (P5-E07): подсказки рейки обещают сочетания `G D`, `G M`, `G O`, `G T`, `G C`, а зарегистрированы только `G H`, `G I`, `G F`; шпаргалка «Горячие клавиши» перечисляет `⌘S`, `/`, `Enter`, `A`, `R`, у которых нет обработчиков, и не знает о реально работающих `S`/`E` во Входящих и клавишах календаря. | В руководстве перечислены только работающие сочетания. Открыто: довести сочетания до обещанного или привести подсказки и шпаргалку к действительности. **Закрыто:** обещанные сочетания зарегистрированы, из шпаргалки убраны несуществующие и добавлены действующие клавиши Входящих. | до пилота |
| N85 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (P5-E07): вход в консоль администрирования (рейка, палитра, мобильное меню) открыт только по способности «Администрирование системы», хотя разделы размечены под `gis.basemaps.manage`, `data.sources.manage`, `processes.manage`, `automation.manage`. «Администратор ГИС», «Ответственный за данные» и «Аудитор безопасности» попадают в свои разделы только прямым адресом `/admin`. | В руководстве администратора это оговорено. Открыто: открывать ли вход в консоль по любой из способностей её разделов (тогда лишние разделы просто не показываются) или убрать разметку и считать консоль целиком принадлежностью администратора системы. | до пилота |
| N86 | **Ответ владельца 24.09.2026 — §5.** К фазе 5 (P5-E07): в консоли нет правки подразделения (переименование, перенос, вид, порядок, территория), назначения руководителя, справочника должностей и управления группами — всё это есть в API, но экранов нет. | Рабочее допущение: оргструктура ведётся синхронизацией с каталогом LDAP, а разовые правки делает разработчик запросом. Открыто: нужны ли экраны до пилота и кто ведёт структуру в организации. **Закрыто 25.09.2026 (ADR-0144):** подразделение правится в консоли (название на трёх языках, код, вид, место в дереве, руководитель, признак действующего). Там же — справочник должностей (правка, удаление свободной), раздел «Группы» (создание, переименование, состав с записью в аудит) и «Подразделение и должность» сотрудника. Перевод закрывает прежнее назначение: раньше сотрудник сохранял доступ к данным прежнего подразделения, следы исправлены миграцией. Только через API остаются порядок и территория подразделения, перетаскивание дерева; удаления групп нет намеренно. | до пилота |
| N87 | К фазе 5 (P5-E07): конструктор правил качества данных задаёт только ключ, вид, поле и важность. Правила «Число в диапазоне», «Соответствие шаблону», «Значение из набора», «Есть в справочнике» и «Данные свежие» сохраняются, но проверка возвращает ошибку — сервер ждёт параметров, которых в интерфейсе не ввести. | В руководстве пользователя перечислено, какие виды правил работоспособны из интерфейса. Открыто: достроить конструктор параметрами или убрать из списка виды, которые нельзя настроить. **Закрыто:** у каждого вида правила свои параметры в конструкторе; сквозной сценарий проверяет диапазон. | до пилота |
| N88 | **Решение исполнителя 24.09.2026 — §5.** К фазе 5 (P5-E07): руководства живут в двух местах — `docs/06-guides/` (полные) и страницы базы знаний в сиде (краткие, ADR-0124). | Рабочее допущение: владелец текста — тот же, кто ведёт `docs/`; сверка при каждом изменении словаря `ru` и при закрытии эпика. Открыто: кто отвечает за руководства в организации, нужен ли перевод на таджикский и английский и нужен ли в интерфейсе пункт «Справка», ведущий на страницу базы знаний. | до пилота |
| N90 | К фазе 5 (P5-E08, ADR-0128): пакет ЧС при повторной установке досоздаёт только недостающие объекты и не трогает найденные — администратор мог их поменять. Исправленные в следующей версии пакета правила, дашборды и формы на установленную систему сами не приедут. | Рабочее допущение: до пилота пакет обновляется переустановкой демо-стенда, а на рабочей установке изменения вносит администратор по списку в журнале изменений пакета. Открыто: нужен ли шаг «Обновить пакет», который накатывает новую версию на объекты, не менявшиеся после установки (отпечаток определения в `meta`), и показывает расхождения по изменённым. | до пилота |

## 5. Ответы владельца продукта (24.09.2026)

Владелец ответил на открытые вопросы 24.09.2026. Там, где он поручил решить «как правильно», решение
принял исполнитель — это оговорено в строке. В таблицах §2 и §4 у каждого отвеченного вопроса стоит
отметка со ссылкой сюда.

### Направление работ

| Вопрос | Решение |
|---|---|
| Релиз | Фазы 1–5 сливаются в `main`, на вершинах фаз ставятся теги v0.2.0…v0.6.0, `main` и теги уходят в `origin`; ветки форков удаляются. |
| Приоритет | Предметный пакет ЧС (P5-E08) вместе с лентами опасных явлений, затем таджикский интерфейс, затем доводка к пилоту. |
| Демонстрация и пилот | Сначала демонстрация Комитету на демо-данных; срок — «когда всё будет готово». Стенд — ноутбук владельца или арендованный VPS, запуск должен подходить для обоих. |
| A1, Q1 | Подтверждено 23.09.2026: заказчик — Комитет по чрезвычайным ситуациям и гражданской обороне при Правительстве Республики Таджикистан. |
| A4, Q10 | S1: один сервер, Docker Compose. Вопросы S2 (N71–N76) отложены до перехода на S2. |
| A6 | У серверов Комитета есть исходящий доступ в интернет через прокси, входящего нет. |
| Q13 | Название для пользователей — «Портал КЧС». **Сделано:** экран входа, заголовок окна, рейка, приложение на телефоне, пуш-уведомления, бот Telegram, коды второго фактора и ключи входа, отправитель писем; техническое имя `kchs` осталось в командах и пакетах. |

### Пакет ЧС (P5-E08, `01-product/04-domain-pack-emergency.md`)

| Вопрос | Решение |
|---|---|
| Виды ЧС | Все четыре направления сразу: паводки и сели, землетрясения, оползни и лавины, техногенные ЧС и пожары. |
| Источники данных | Данные Гидромета и сейсмослужбы пока вносятся вручную формами, для демонстрации — синтетические ряды. Интеграции — после договорённости со службами. |
| Ленты опасных явлений | Входят в пакет уже для демонстрации: GDACS, USGS, EMSC, NASA FIRMS с привязкой к району и уведомлением дежурной смены (компонент «Мониторинг опасных явлений» концепт-ноты). Предупреждений населению система не выпускает. |
| Q5, образцы форм | Сводка, донесение, приказ и протокол штаба — по типовой практике служб ЧС стран СНГ; Комитет поправит после демонстрации. |
| N49 | Суточная сводка — итоги цифрами и список происшествий за сутки: нужны табличные формы сбора (много строк за период). Сделано: ADR-0129. |
| Территории | Области и районы (города) с границами; джамоаты и населённые пункты — не в первой версии. |
| N5, Q6 | Границы — из открытых данных (geoBoundaries или OCHA) с лицензией в `seeds/README`; официальные заменят их импортом с сохранением кодов. |
| N7 | Население — опубликованная статистика (Агентство по статистике или ООН) с источником и годом в `seeds/README`. |

### Инфраструктура, доступ, язык

| Вопрос | Решение |
|---|---|
| N6, Q7 | ИИ пока выключен, как сейчас: провайдер включается отдельным решением. |
| N27 | Сервера с GPU нет. Расшифровка встреч — на процессоре, по желанию администратора; таджикскую речь не включаем, пока владелец не примет качество на своих записях. |
| N43, N44 | Каталога LDAP/AD и IdP у Комитета нет: учётные записи и оргструктура ведутся в kchs. Вопросы каталога и единого входа — когда он появится. |
| N86 | Раз каталога нет, экраны должностей и назначений сотрудников в консоли нужны до пилота. |
| N3 | Второй фактор по умолчанию обязателен для ролей «Администратор системы» и «Аудитор безопасности». |
| N46, Q3 | Таджикский: исполнитель переводит по глоссарию, носитель языка из Комитета вычитывает. Порядок: вход, «Мой день», Входящие → документы и поручения → пакет ЧС → остальное. |
| N4 | Как сейчас: плавающие праздники и переносы вносит администратор в разделе бизнес-календаря консоли. |
| N23 | Тихие часы и «не беспокоить» глушат внешние каналы (Telegram, почта) по всем категориям, кроме срочного: алерты ЧС, эскалации и срочные поручения проходят всегда. |
| Q9, N41, N56 | Интеграций с 1С и кадровой системой пока нет. Нужна полноценная почта Комитета: почтовый сервер в составе установки, ящики сотрудников и канцелярии, веб-почта, домен Комитета с записями MX, SPF и DKIM. |

### Документооборот

| Вопрос | Решение |
|---|---|
| A7, Q4 | Простой электронной подписи достаточно; квалифицированная — позже, через порт `SignatureProvider`. |
| N10 | Гриф «Секретно» убрать: гостайна обрабатывается только в аттестованных системах. Остаются «Для служебного пользования» и «Конфиденциально». Это правка контракта `08-documents.md` — ADR и миграция. |
| N11 | Как сейчас: ДСП видит каждый, кому дан доступ к документу, «Конфиденциально» — только с допуском. |
| N12 | Номер по умолчанию — «подразделение-дело/номер» (`03-12/145`): код подразделения, индекс дела по номенклатуре, порядковый номер. |
| N8, N9 | Как сейчас. |
| N14 | Решение исполнителя по поручению владельца («как правильно, но не трудно пользователям»): стартовые маршруты остаются как в сиде — юрист по выбору инициатора, подпись с кодом для приказов и исходящих, служебная записка без кода, сроки 3/2/2 рабочих дня; в пакет ЧС добавляется экстренный маршрут со сроками в часах. |
| N15–N19 | Как сейчас. |
| N20 | В демо — типовая номенклатура по подразделениям и импорт номенклатуры из Excel для канцелярии. |
| N22 | После отправки ответа исполнитель видит готовый отчёт с номером исходящего и отправляет его одной кнопкой; принимает автор резолюции. |
| N65–N67 | Решение исполнителя по поручению владельца («как правильно принято»): ответы письмом в обсуждение не делаем — адрес отправителя слабо удостоверяет личность; для доменов ведомств из белого списка корреспондент подставляется по домену; отклонённые и неразобранные письма очереди «Из почты» удаляются через 180 дней. |
| N68, N69 | Как сейчас: ONLYOFFICE Community в контуре Комитета, конфиденциальные файлы правятся скачиванием. |

### Встречи, знания, автоматизация

| Вопрос | Решение |
|---|---|
| N29, Q11 | Записи встреч хранятся 6 месяцев. За неделю до удаления организатор получает предупреждение и может закрепить запись; записи, попавшие в протокол или дело, не удаляются. |
| N30 | Протокол правят организатор и назначенный им секретарь встречи, остальные читают и комментируют. |
| N32 | Печатная форма протокола (PDF с повесткой, решениями и поручениями) становится первой версией документа — подписывают её. |
| N33 | Поручению из протокола без названного срока ставится 10 рабочих дней, организатор может изменить. |
| N34, N36 | Как сейчас. |
| N35 | Регламентам и инструкциям срок пересмотра ставится сам — год от публикации; при просрочке больше месяца читатель видит предупреждение. |
| N89 | Страница знаний из отчёта не нужна: сценарий C завершается исходящим документом. |
| N37, N47 | Правила и формы работают от отдельных служебных учётных записей: без входа, без уведомлений, с отметкой в справочнике; назначить `run_as` живого сотрудника нельзя. Сделано для правил — ADR-0130; `run_as` форм подключается отдельно. |
| N38 | Письма из правил — только на адреса организации и домены белого списка, вебхуки — только на разрешённые домены; список ведёт администратор. |
| N40, N77 | Как сейчас: токены API и правила с вебхуками — только у администратора системы. |

### Решения исполнителя по остальным вопросам

Эти вопросы владельцу не задавались: они технические, или ответ следует из решений выше.

| Вопрос | Решение |
|---|---|
| N2, N13, N21, N25, N26, N28, N31, N39, N50, N51, N53–N55, N57–N64, N78 | Рабочие допущения остаются в силе. |
| N24 | Закрыт при приёмке фазы 4 (`LIVEKIT_NODE_IP`); внешний адрес для установок за NAT задаётся при установке. |
| N42 | Стабильный ключ журналов — вместе с переносом настройки между контурами. |
| N45 | Каталога нет, но кнопка администратора «отозвать ключи входа сотрудника» с записью в аудит нужна и для локальных учётных записей — при доводке к пилоту. |
| N48 | В назначении формы появляется ответственный за сдачу, руководитель подразделения остаётся запасным вариантом. Сделано: ADR-0129. |
| N52 | Закрыт 20.09.2026: исправлено при слиянии первой волны фазы 5. |
| N70 | Карточка документа показывает «файл сейчас правят» — при доводке к пилоту. |
| N71–N76 | Отложены до перехода на S2. |
| N79 | Потолок времени для всех запросов к внешним базам (`statement_timeout`, `MAX_EXECUTION_TIME`) — при доводке к пилоту. |
| N80 | Обновить `drizzle-orm` до 0.45 и `nodemailer` до актуальной версии; `nodemailer` — вместе с почтовым сервером. |
| N81 | Команда `kchs secrets rotate` — до пилота. |
| N82 | Брошенные попытки входа чистятся ежедневным заданием обслуживания — при доводке к пилоту. |
| N85 | Консоль открывается по любой способности её разделов, лишние разделы скрыты — при доводке к пилоту. |
| N88 | Пункт «Справка» ведёт на страницу базы знаний; руководства переводятся вместе с интерфейсом. |

### Что ещё нужно от владельца

- Домен Комитета и публичный адрес для почтового сервера (записи MX, SPF, DKIM).
- Носитель языка из Комитета — вычитать перевод и утвердить глоссарий.
- Когда появятся: официальные образцы форм, номенклатура дел, официальные границы и численность
  населения. Они заменят типовые без переделки.
