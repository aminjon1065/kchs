# Риски и открытые решения

## 1. Архитектурные и технические риски

| # | Риск | Влияние | Митигация |
|---|---|---|---|
| R1 | Широта продукта → незавершённые модули | потеря целостности | ядро первым; фазы с DoD; вертикальные срезы; никаких параллельных «полустроек» |
| R2 | Компилятор запросов: ошибки типизации/SQL, обход политик строк | безопасность, доверие | golden-тесты; единственный путь к данным; libpg-query для SQL; роль `kchs_query`; негативные тесты по всем путям |
| R3 | Производительность собственного DataGrid | UX аналитика | ранние бенчмарки (P1-E03); запасной путь — AG Grid Community за тем же интерфейсом (ADR-0008) |
| R4 | Тайлы «на лету» на больших слоях | медленные карты | генерализация, кластеризация, кэш, Martin в S2, deck.gl |
| R5 | Совместное редактирование (Yjs) и права | конфликты, утечки | ограниченный набор объектов; права на уровне Hocuspocus; тесты |
| R6 | LiveKit: сеть/NAT/TURN, запись | звонки не работают в части сетей | TURN/TLS на 443; предварительная проверка сети в клиенте; документ по портам для ИТ-службы |
| R7 | Качество расшифровки таджикской речи | протоколы неточны | ручной режим; выбор модели; оценка на пилоте; возможна дообученная модель |
| R8 | LLM-провайдер: стоимость, недоступность, приватность | функции ИИ | сменяемый провайдер; on-prem вариант; кэш; лимиты; всё ИИ — опционально |
| R9 | Один Postgres для всего | узкое место при росте | партиционирование, реплика чтения, колоночный tier; мониторинг с ранними порогами |
| R10 | Движок процессов: сложные маршруты, долгие ожидания | баги оркестрации | событийная модель шагов, идемпотентность, тесты восстановления; Temporal как план Б |
| R11 | Модель доступа с политиками типов — сложность | ошибки прав | таблицы истинности, генерируемые негативные тесты, «объяснить доступ» как инструмент отладки |
| R12 | Локализация таджикского (терминология, ИИ, OCR) | принятие пользователями | словарь терминов в глоссарии с ранней проверкой носителем; ключи с fallback на ru |
| R13 | Геометрии административных границ: источник и лицензия | юридический | зафиксировать источник в seeds/README, предпочесть официальные данные |
| R14 | Электронная подпись: юридическая сила | документооборот «не считается» | порт `SignatureProvider`; простая ЭП как внутренний инструмент; уточнить требования |
| R15 | Объём фронтенда (десятки экранов) | производительность, время | ленивые модули, единые компоненты, бюджеты бандла |
| R16 | Python-движок: тяжёлый образ, модели | развёртывание | образ с моделями отдельным слоем; профили compose; документ по ресурсам |

## 2. Открытые продуктовые вопросы (с рабочим вариантом)

| # | Вопрос | Рабочий вариант | Кому/когда уточнить |
|---|---|---|---|
| Q1 | Предметная область — КЧС? Какие типы ЧС, источники, формы? | платформа нейтральна; предметный пакет — конфигурацией (фаза 5) | владелец продукта до фазы 2 (влияет на seed и территории) |
| Q2 | Одна организация на инсталляцию? | да | до фазы 0 (влияет на модель) |
| Q3 | Языки и приоритет таджикского | ru основной, tg/en | до фазы 0 |
| Q4 | Требования к ЭЦП | простая ЭП, порт для квалифицированной | до фазы 3 |
| Q5 | Официальные формы журналов, печатных форм, номенклатуры дел | стандартные CIS-практики | до фазы 3 |
| Q6 | Источник границ территорий и классификатор кодов | открытые данные с фиксацией лицензии | до фазы 1 |
| Q7 | Политика использования облачного ИИ | сменяемый провайдер, по умолчанию on-prem-совместимо | до фазы 1 |
| Q8 | Нужны ли мобильные нативные приложения и оффлайн-полевой сбор | PWA; полевой сбор — фаза 3+ | до фазы 4 |
| Q9 | Интеграции с существующими системами (1С, HR, почта, AD) | SMTP/IMAP, LDAP, OIDC; остальное — по запросу | до фазы 5 |
| Q10 | Масштаб и инфраструктура (число пользователей, данных, серверов) | S1 | до фазы 0 (влияет на compose-профиль) |
| Q11 | Требования к хранению сообщений и записей (сроки, аудит) | бессрочно, политика в админке | до фазы 4 |
| Q12 | Межведомственный обмен документами | точка расширения | по требованию |
| Q13 | Название продукта для пользователей и брендирование | «kchs» до уточнения; брендирование в админке | до релиза фазы 0 |

## 3. Открытые технические решения (исполнитель решает, фиксирует ADR)

- Точный формат состояния вкладок и стратегия keep-alive тяжёлых панелей.
- Библиотека drag-and-drop и виртуализации в списках (рекомендованы `@dnd-kit`, TanStack Virtual).
- Хранение результатов профилирования столбцов (таблица vs jsonb в датасете).
- Способ материализации `event_instances` календаря.
- Диаризация спикеров (pyannote — лицензия/ресурсы) — включать ли.
- Кэш тайлов: только Redis или диск/nginx с первого дня.
- Генерация OpenAPI-клиента: `openapi-fetch` vs `orval`.

## 4. Новые вопросы (заполняет исполнитель по ходу работы)

| # | Вопрос | Принятое рабочее допущение | Когда нужен ответ |
|---|---|---|---|
| N1 | ~~Среда разработки: git недоступен (не приняты соглашения Xcode Command Line Tools).~~ **Снят 18.09.2026**: история ведётся; фаза 0 — исходный коммит в `main` и ветка `phase/0-closeout`. | — | — |
| N2 | Учётные данные демонстрационной установки: администратор `admin` / `Kchs!Start-2026-7q`, сотрудники `user001…user060` / `Kchs!Work-2026-3v`. | Пароли задаются переменными `SEED_ADMIN_PASSWORD`, `SEED_USER_PASSWORD`; для боевой установки обязательна смена при первом входе. | до пилота |
| N3 | Требуется ли принудительная MFA для ролей `system_admin` и `security_auditor` с первого дня? | MFA доступна всем, принудительность отключена; включается политикой `security.requireMfaRoles` — механизм реализован (ADR-0040), администратор отмечает роли в консоли «Безопасность». Открыто только значение по умолчанию. | до релиза фазы 0 |
| N4 | Производственный календарь РТ: какие дни `kchs init` должен заносить сам? | Заносятся праздники с постоянной датой по Трудовому кодексу РТ (1 января, 8 марта, 21–24 марта, 1 и 9 мая, 27 июня, 9 сентября, 6 ноября) на текущий и следующий год; Иди Рамазон, Иди Қурбон и переносы выходных объявляются постановлением Правительства — их вносит администратор, когда появится раздел календаря в консоли (ADR-0044). Открыто: нужен ли источник для автоматической загрузки или утверждённый список на годы вперёд. | до пилота |
| N5 | К фазе 1 (Q6): источник территорий и классификатор кодов. P1-E07 S03 нужны территории для группировки и политик строк «только своя область». | Фаза 1 обходится без геометрий: справочник территорий из seed — 5 единиц первого уровня с кодами ISO 3166-2:TJ (TJ-DU Душанбе, TJ-SU Согдийская, TJ-KT Хатлонская, TJ-GB ГБАО, TJ-RA РРП) и районы демо-данных со своими устойчивыми кодами; при загрузке реальных данных коды сопоставляются справочником. Геометрии — к фазе 2 из открытых данных (geoBoundaries, CC BY 4.0) с лицензией в `seeds/README`, пока владелец не укажет официальный источник. | до загрузки реальных данных (пилот фазы 1); геометрии — до фазы 2 |
| N6 | К фазе 1 (Q7): можно ли отправлять данные во внешний (облачный) ИИ? P1-E09 S03 «Спросить данные» строит запрос по вопросу на русском. | ИИ по умолчанию выключен (`AI_PROVIDER` пуст — функция скрыта, 13-search-knowledge-ai.md). Администратор включает облачного провайдера или on-prem (OpenAI-совместимый эндпоинт). Во внешний провайдер уходят схема датасета, профиль столбцов и агрегаты, не строки; каждый вызов — в аудит; запрос показывается пользователю и исполняется компилятором `packages/query` с политиками строк. | до включения на пилоте |
| N7 | К фазе 2: источник численности населения по административным единицам — хороплет «на 1 000 жителей» (P2-E04 S04, ADR-0077) и показатели паспорта территории. | Население — атрибут `population` единицы справочника территорий (столбец `population` системного датасета «Территории»); в демо-данных — приблизительные значения сида. Реальные значения загружаются импортом справочника; единица без населения в хороплете получает пустое значение, а не ошибку. Открыто: официальный источник (Агентство по статистике при Президенте РТ?) и год, на который берутся данные. | до загрузки реальных данных |
| N8 | К фазе 3: семантика согласования в маршрутах (ADR-0079). | «Отклонить» завершает параллельный шаг сразу, «Замечания» ждут ответов всех назначенных; при повторном согласовании «только отклонивших» (`rejecters_only`) заново согласуют все, кто не одобрил, включая не успевших ответить. Открыто: подтвердить или задать другую практику канцелярии. | до приёмки фазы 3 |
| N9 | К фазе 3: срок шага последовательного согласования (ADR-0079). | Срок общий на шаг: очередь согласующих делит один срок от активации шага. Открыто: нужен ли каждому согласующему свой срок от момента его очереди. | до приёмки фазы 3 |
| N10 | К фазе 3 (08-documents.md §13): применим ли гриф «Секретно» в системе? Сведения, составляющие государственную тайну, обычно обрабатываются только в аттестованных системах. | Гриф `secret` реализован наравне с остальными (ADR-0080), но ни у одного стартового типа, кроме «Донесения», он не разрешён; допуск `secret` администратор выдаёт вручную. Открыто: убрать `secret` из допустимых значений или оставить для служебных пометок, не являющихся гостайной. | до пилота документооборота |
| N11 | К фазе 3: допуск сотрудников по умолчанию и порог обезличивания уведомлений. | Допуск по умолчанию — «Для служебного пользования» (`internal`): все сотрудники видят документы ДСП, если им дан доступ; «Конфиденциально» — только с выданным администратором допуском. Уведомления, письма, Telegram и Входящие скрывают тему документа начиная с «Конфиденциально»; ДСП — с темой. | до пилота документооборота |
| N12 | К фазе 3: официальные форматы регистрационных номеров и состав журналов для органов РТ. | Стартовые журналы и шаблоны номеров (`ВХ-0001/26`, `001-ПР/26` …) — редактируемый сид; делопроизводитель меняет префикс, шаблон и сброс счётчика в справочнике журналов. Открыто: нужен ли индекс подразделения (`{unit.code}`) и номенклатурный индекс дела в номере по умолчанию. | до пилота документооборота |
| N13 | К фазе 3 (календарь, ADR-0081): какие календари показывать по умолчанию. Сотрудник видит календари всех пространств, где он участник; администратор системы участвует во всех пространствах подразделений — у него отмечено больше двадцати календарей. | Показываются личный календарь, календари пространств пользователя, проекты и подписки; ресурсы — только по отметке. Пользователь снимает отметки в левой колонке (запоминаются). Открыто: нужен ли набор по умолчанию «только своё подразделение» для руководителей и администраторов. | до пилота фазы 3 |
| N14 | К фазе 3 (маршруты, ADR-0083): состав стартовых маршрутов. Исходящее письмо — «юрист и профильный отдел» выбирает инициатор (ролей «юрист пространства» в оргструктуре нет), затем руководитель руководителя подразделения автора, подпись подписанта из карточки с кодом, регистрация канцелярией; приказ — согласующие по выбору, подпись с кодом, номер сразу после подписи; служебная записка — подпись руководителя подразделения без кода. | Маршруты редактируемые (новая версия определения не трогает идущие); сид их не перезаписывает. Открыто: утвердить состав и сроки (3/2/2 рабочих дня), нужна ли подпись с кодом для приказов и записок, нужен ли юрист всегда (роль `legal` в пространстве). | до пилота документооборота |
| N15 | К фазе 3: согласование конфиденциальных документов. Согласующий или подписант без допуска к грифу документа шаг не получает — маршрут не запускается («нет допуска: …»), на ходу шаг не активируется. | Альтернатива — показывать такому участнику только карточку без содержания — отклонена: решение вслепую. Открыто: допустимо ли, что руководитель без допуска не может подписать конфиденциальный документ (выдать допуск или назначить другого подписанта). | до пилота документооборота |
| N16 | К фазе 3 (резолюции, ADR-0084): кто вносит резолюции за руководителя и кто контролёр по умолчанию. В оргструктуре нет роли «помощник руководителя». | Резолюцию от имени руководителя вносит делопроизводитель (право регистрации документа) или заместитель «от имени»; контролёр по умолчанию — контролёр документа, иначе зарегистрировавший его. Открыто: нужна ли роль «помощник руководителя» с правом вносить резолюции только своего руководителя и быть контролёром по умолчанию. | до пилота документооборота |
| N17 | К фазе 3 (ознакомление, ADR-0084): кого знакомить с приказами по умолчанию и нужна ли юридически значимая отметка. | Приказы и распоряжения при регистрации — сотрудникам подразделения документа (правило типа можно изменить: подразделения, срок, код), срок — 3 рабочих дня; отметка — кнопкой, с кодом второго фактора — по настройке типа или запроса. Открыто: знакомить ли со всеми приказами всю организацию и требовать ли код для приказов по основной деятельности. | до пилота документооборота |
| N18 | К фазе 3 (08-documents.md §5, ADR-0085): реквизиты и место штампа регистрации входящего. | Штамп — в правом нижнем углу первого листа копии PDF (ГОСТ Р 7.0.97, «отметка о поступлении»): название организации, «Вх. № …», дата регистрации; накладывается сам после регистрации, версия документа не меняется. Открыто: нужны ли время поступления, число листов, индекс подразделения, другое место (верхний правый угол) или штамп только на бумажном оригинале. | до пилота документооборота |
| N19 | К фазе 3 (08-documents.md §13, ADR-0085): кто может скачать исходник файла с грифом «Конфиденциально» без водяного знака. | Только администратор системы в режиме администратора — с аудитом; все остальные, включая автора и делопроизводителя, скачивают копию с водяным знаком (имя, время). Открыто: нужно ли право «выдачи исходника» автору или канцелярии, и какой текст водяного знака принят (сейчас: гриф, ФИО, дата и время). | до пилота документооборота |
| N20 | К фазе 3 (дела и архив, ADR-0086): номенклатура дел и сроки хранения. Нужна утверждённая номенклатура дел Комитета и перечень сроков хранения документов, по которым канцелярия заводит дела и составляет акты о выделении к уничтожению. | Номенклатуру ведёт канцелярия в системе вручную (индекс, заголовок, год, срок в годах или «постоянно», статья перечня текстом); срок считается с 1 января года, следующего за годом дела; акт составляет владелец «вести журналы» с текстовым основанием (протокол экспертной комиссии). Демо-сид заводит пример номенклатуры со сроками 3, 5 лет и «постоянно». Открыто: импорт утверждённой номенклатуры, согласование акта экспертной комиссией маршрутом, передача дел в государственный архив. | до пилота документооборота |
| N21 | К фазе 3 (ADR-0086): правила исполнения без резолюции. Исходящий исполняется отправкой; зарегистрированный документ без контроля («к сведению») делопроизводитель подшивает в дело сразу. | Первая отметка об отправке переводит исходящий в «Исполнен»; подшивка зарегистрированного без контроля — переход «не требует исполнения» (`registered → executed → filed`); документ на контроле ждёт исполнения. Закрытое дело можно открыть снова, пока оно не передано в архив. Открыто: подтвердить практику канцелярии; нужна ли отметка «В дело» руководителем (резолюцией) вместо решения делопроизводителя. | до пилота документооборота |
| N22 | К фазе 3 (сценарий B, шаг 6; ADR-0084, ADR-0086): закрывать ли поручение по резолюции автоматически, когда ответ «в ответ на» входящий зарегистрирован и отправлен. | Нет: ответственный отчитывается сам (в отчёте — номер исходящего), автор резолюции принимает отчёт; входящее исполняется по закрытию последнего поручения. Открытые направления на резолюцию закрытого документа (исполнен, подшит, в архиве) снимаются сами. Открыто: нужен ли автоотчёт по отправке ответа и кто тогда принимает его — автор резолюции или контролёр. | до пилота документооборота |
| N23 | К фазе 4 (ADR-0090): тихие часы и «не беспокоить» действуют только на уведомления чатов — статус присутствия. Распространять ли их на все категории (поручения, согласования, напоминания о встречах) и с каким исключением для срочного? | Тихие часы и «не беспокоить» из `/me/presence` глушат внешние каналы (Telegram, почта) для `chat.*`; значок в приложении остаётся всегда. Поля `quietHours`/`doNotDisturbUntil` в настройках уведомлений ядра пока не применяются. | до приёмки фазы 4 |
| N24 | К фазе 4 (ADR-0091): медиасервер в compose объявляет адрес контейнера (`use_external_ip: false`), и браузер на хосте с Docker Desktop (macOS) до потока не доходит — ICE обрывается через несколько секунд. | Решено при приёмке фазы: у службы `livekit` появился необязательный `LIVEKIT_NODE_IP` (флаг `--node-ip`). На стенде разработчика в него ставят адрес хоста в сети (не `127.0.0.1`: его не видит Egress из своего контейнера), и тогда работают и браузер на хосте, и запись. В установке переменная пуста — узел определяет адрес сам. Открыто: нужен ли отдельный внешний адрес (`use_external_ip`) для установок за NAT. | до пилота фазы 4 |
| N25 | К фазе 4 (ADR-0091): кого пускать в комнату ожидания. Сейчас ждут только гости по ссылке, приглашённые участники входят сразу по праву `join`. | Участник системы не ждёт: его право уже проверено, а организатора в комнате может не быть. Открыто: нужен ли режим «ждут все, кроме организатора» для больших совещаний. | до пилота фазы 4 |
| N26 | К фазе 4 (ADR-0091): срок гостевой ссылки по умолчанию — 4 часа (от 15 минут до суток), отзыва ссылки отдельной кнопкой нет: она обесценивается завершением встречи. | Достаточно для приглашения на встречу дня. Открыто: нужен ли журнал выданных ссылок и их досрочный отзыв (тогда ссылка станет записью в базе, а не подписанным токеном). | до пилота фазы 4 |
| N27 | К фазе 4 (ADR-0092): какая модель распознавания речи ставится на сервер и хватает ли её для таджикского. Качество для таджикского — известный риск (Q9 ниже): у Whisper таджикский покрыт слабо. | Модель задаётся `ENGINE_TRANSCRIBE_MODEL` и по умолчанию пуста — расшифровка выключена, интерфейс честно говорит «модель не настроена», ручной режим (слушать запись) остаётся. Рекомендуемое начало — `large-v3` на процессоре с `int8` для русского и английского; для таджикского расшифровку лучше не включать, пока владелец не примет качество на своих записях. Открыто: какая модель и какое железо выделяются, включать ли таджикский вообще. | до пилота встреч |
| N28 | К фазе 4 (ADR-0092): нужна ли диаризация (кто говорит) в расшифровке и протоколе. Сейчас поле спикера пустое: composite-дорожка комнаты сводит всех в один канал, а отдельная модель (pyannote) требует своих весов, токена и времени, сопоставимого с расшифровкой. | Расшифровка идёт без спикеров; поле в контракте есть и заполнится, если появится запись по участникам (отдельная дорожка на человека) или отдельная модель. Черновик протокола сможет опираться на список участников встречи, а не на спикеров. Открыто: нужна ли диаризация для протоколов и приемлемо ли удвоение времени обработки. | до пилота протоколов |
| N29 | К фазе 4 (ADR-0092): срок хранения записей встреч и кто может их удалять. Час записи комнаты 720p — примерно 0,5–1 ГБ; десяток совещаний в неделю заметно растит хранилище. | Записи хранятся бессрочно как обычные файлы реестра; удалить запись может владелец (организатор встречи) или администратор. Открыто: нужен ли автоматический срок (например, 6 месяцев) с предупреждением организатору и исключением для записей, попавших в протокол или дело. | до пилота встреч |
| N30 | К фазе 4 (протокол, ADR-0093): кто ведёт протокол. Сейчас повестку и протокол правит любой участник встречи (уровень `comment`), подтверждает организатор. | Совместная правка — всем участникам: протокол пишут по ходу встречи, секретаря как роли в системе нет. Открыто: нужна ли роль «секретарь встречи» (право правки без права входа в комнату) и надо ли закрывать правку всем, кроме организатора и секретаря. | до пилота фазы 4 |
| N31 | К фазе 4 (ADR-0093): что можно править после подтверждения протокола. Сейчас текст закрыт: поручения уже ушли исполнителям. | Исправление — новым блоком с оговоркой или новой версией зарегистрированного документа. Открыто: нужна ли канцелярская практика «протокол с изменениями» (повторное подтверждение с версией и отметкой, кто и что изменил). | до пилота фазы 4 |
| N32 | К фазе 4 (ADR-0093): каким типом документа регистрируется протокол и нужен ли ему файл. Сейчас человек выбирает тип (стартовый «Протокол»), текст протокола попадает в краткое содержание, файла версии нет — печатная форма протокола не сделана. | Документ заводится черновиком, дальше — обычный маршрут согласования и подписи. Открыто: нужна ли печатная форма протокола (PDF с повесткой, решениями и поручениями) как версия документа и обязательна ли она для подписи. | до пилота фазы 4 |
| N33 | К фазе 4 (ADR-0093): срок поручений из протокола, если он не назван на встрече. Сейчас подтвердить блок без срока нельзя. | Организатор проставляет срок руками; ИИ предлагает только названную дату. Открыто: нужен ли срок по умолчанию (например, 10 рабочих дней от подтверждения) и общий срок исполнения протокола. | до пилота фазы 4 |
| N34 | К фазе 5 (база знаний, ADR-0095): кто ведёт базу знаний и кто публикует. Сейчас страницу правит любой, у кого есть право `edit` в пространстве, а публикует, назначает владельца и срок пересмотра — уровень `manage` (у автора он есть: он владелец объекта). | Так база знаний живёт как вики: пишут многие, отвечает за опубликованный текст владелец страницы. Открыто: нужна ли роль «редактор базы знаний» на пространство и следует ли закрыть правку опубликованной страницы всем, кроме владельца. | до пилота фазы 5 |
| N35 | К фазе 5 (ADR-0095): срок пересмотра по умолчанию и что делать с просроченной страницей. Сейчас срок не назначается сам, а по его наступлении страница уходит в «на пересмотре» и владельцу открывается дело; из выдачи и печати она не исчезает. | Молчаливое устаревание хуже видимого: читатель видит отметку «на пересмотре», но текст остаётся доступен. Открыто: нужен ли срок по умолчанию (например, год от публикации) для регламентов и инструкций и нужно ли предупреждать читателя отдельно, если пересмотр просрочен больше чем на месяц. | до пилота фазы 5 |
| N36 | К фазе 5 (ADR-0095): что должно попадать в PDF страницы. Сейчас печатаются текст, таблицы и картинки; график, карта, таблица датасета, показатель и список задач заменяются строкой со ссылкой на объект — их содержимое строится в браузере, а страница печати документов работает с закрытой сетью. | Регламент печатается ради текста, а встроенные представления живут в системе. Открыто: нужен ли для страниц путь печати через страницу в Chromium (как у отчётов, ADR-0078), чтобы графики и карты попадали в PDF картинками. | до пилота фазы 5 |
| N46 | К фазе 5 (DoD §3): с этой фазы заглушек таджикского недостаточно, а сейчас `tg` покрыт на 6 % (324 ключа из 5310). Перевод интерфейса — работа не программиста: нужен глоссарий терминов и носитель языка. | Допущение по умолчанию: машинный перевод по глоссарию с вычиткой носителем, ключи без перевода падают на `ru` (механизм уже есть). Открыто: кто вычитывает, нужен ли таджикский в первом пилоте и какие разделы важнее (вход, Входящие, документы). | до приёмки фазы 5 |
| N43 | К фазе 5 (ADR-0098): что считать признаком отключённой записи в каталоге заказчика и по какому атрибуту сопоставлять сотрудника между прогонами. По умолчанию — `userAccountControl` (бит 2) и `objectGUID` у Active Directory; у OpenLDAP признак отключения бывает `nsAccountLock`, `pwdAccountLockedTime` или отсутствует вовсе, а устойчивый идентификатор — `entryUUID`. | Оба атрибута настраиваются в разделе «Каталог» и проверяются кнопкой «Проверить соединение» и предпросмотром; умолчания взяты по Active Directory. Открыто: какой каталог в организации, как в нём помечается уволенный и есть ли у записей устойчивый идентификатор — без него сотрудник опознаётся по логину и переименование в каталоге заведёт вторую учётную запись. | до подключения каталога на пилоте |
| N44 | К фазе 5 (ADR-0098): распространяется ли требование второго фактора платформы (`security.requireMfaRoles`) на вход через корпоративный IdP. Сейчас — да: сотруднику с такой ролью платформа всё равно предложит подключить код приложения или ключ входа, даже если провайдер уже спросил второй фактор. | Гарантия организации не зависит от настройки провайдера; подключить фактор можно ключом входа, без приложения-аутентификатора. Открыто: принять это или доверять провайдеру — тогда нужен список значений `acr`/`amr`, при которых вход считается подтверждённым двумя факторами, и договорённость с администратором IdP о том, что они не подделываются. | до включения единого входа на пилоте |
| N45 | К фазе 5 (ADR-0098): кто и как восстанавливает доступ сотруднику каталога или IdP, потерявшему устройство. У записи каталога локального пароля нет, ссылка восстановления ведёт к смене локального пароля, а ключ входа отзывается только своим владельцем в профиле. | Пароль такому сотруднику меняет администратор каталога или IdP — вне платформы; администратор системы может заблокировать учётную запись и завершить её сессии. Отозвать чужой ключ входа сейчас нельзя ни администратору, ни владельцу утерянного устройства — только с другого устройства. Открыто: нужна ли администратору кнопка «отозвать все ключи входа сотрудника» (с аудитом) и должно ли восстановление по ссылке переводить учётную запись каталога на локальный пароль. | до пилота единого входа |
| N40 | К фазе 5 (ADR-0097): кому разрешать выпуск токенов публичного API. Способность `api_tokens.create` сейчас только у администратора системы, поэтому аналитик или делопроизводитель токен себе не выпустит — придётся просить администратора. | Начали со строгого: токен живёт дольше сессии, вторым фактором не подтверждается и легко утекает в чужой журнал. Открыто: выдавать ли способность ролям `data_steward` и `gis_admin` (интеграции с внешними источниками — их работа) и нужно ли ограничение «не больше N действующих токенов на человека». | до пилота интеграций |
| N41 | К фазе 5 (ADR-0097): какие маршруты открывать токенам. Сейчас область выводится из тега маршрута, а личный кабинет, вход, администрирование и ИИ закрыты при любых областях. | Закрыто по умолчанию: новый модуль сам решает, открывать ли своё API. Открыто: нужен ли интеграциям доступ к администрированию (заведение пользователей из кадровой системы, синхронизация оргструктуры) — тогда понадобится область `write:org` на маршрутах `/admin/*` и отдельная отметка в аудите. | до пилота интеграций |
| N42 | К фазе 5 (ADR-0097): состав пакета конфигурации. Переносятся интеграции, вебхуки и маршруты; типы документов перенести нельзя — карточка типа ссылается на журнал регистрации по UUID, а стабильного ключа у журналов нет (`journals.name` меняется). | Раздел «типы документов» объявлен и в предпросмотре честно помечается «не поддержана». Открыто: добавить ли журналам стабильный ключ (миграция и правка контракта `08-documents.md`) — после этого в пакет поедут типы документов, шаблоны, справочники, показатели, дашборды и карты по той же схеме. | до переноса настройки между контурами |
| N37 | К фазе 5 (правила автоматизации, ADR-0096): кто такие служебные учётные записи правил. Правило работает от имени `run_as` — действующего пользователя без прав администратора системы; отдельного вида «служебная учётная запись» в оргструктуре нет. | Администратор заводит обычного пользователя (например, «Автоматизация канцелярии») с нужными ролями и включает его в нужные пространства; правило не может больше, чем он. Открыто: нужен ли отдельный вид учётной записи (без входа по паролю, без почты и Telegram, видимый в справочнике отдельно) и запрет назначать `run_as` живого сотрудника. | до пилота автоматизации |
| N38 | К фазе 5 (ADR-0096): кому можно писать письма и вызывать внешние адреса из правил. Действие `send_email` принимает и выражения назначений, и явные адреса; `webhook` — любой `https`-адрес. | Правила ведёт владелец способности `automation.manage` (по умолчанию — администратор системы), каждый вызов и письмо попадают в журнал запусков. Открыто: нужен ли белый список доменов для исходящих вызовов и запрет писем на адреса вне организации. | до пилота автоматизации |
| N39 | К фазе 5 (ADR-0096): что делать с правилами при переносе конфигурации между контурами и при удалении служебного пользователя. Ключ правила (`key`) стабилен, но `run_as`, пространства и показатели — идентификаторы установки. | Пока: правило переносится с ключом, `run_as` и ссылки на объекты администратор задаёт заново в целевом контуре; при удалении пользователя `run_as` обнуляется и запуски падают с понятной ошибкой владельцу правила. Открыто: нужен ли экспорт правил в пакет конфигурации (14-automation-integrations.md §6) с сопоставлением служебных пользователей и автоматическое выключение правил с недоступным `run_as`. | до переноса конфигурации между контурами |
| N47 | К фазе 5 (ADR-0103): от чьего имени форма пишет строки датасета. Сейчас — служебный пользователь формы (`run_as`, по умолчанию её создатель): назначенному достаточно видеть форму, права на датасет ему не выдаются. | Альтернатива — выдавать каждому сдающему подразделению право правки датасета; она шире, чем нужно, и открывает правку чужих строк. Открыто: подтвердить схему со служебным пользователем и решить, кто становится `run_as` при уходе сотрудника (сейчас запись просто перестаёт работать с понятной ошибкой). | до пилота сбора сводок |
| N48 | К фазе 5 (ADR-0103): кому адресовать «Сдать сводку», если у назначенного подразделения нет руководителя. Сейчас дело уходит его сотрудникам (не больше десяти). | Рабочее допущение: оргструктура заполняется до начала сбора, и у сдающих подразделений руководитель есть. Открыто: не лучше ли указывать ответственного за сдачу прямо в назначении формы. | до пилота сбора сводок |
| N49 | К фазе 5 (ADR-0103): одна отправка — одна строка датасета (сводка за период). Табличные формы «много строк за период» (список происшествий за сутки) не реализованы. | Допущение: первые формы — числовые сводки. Открыто: нужны ли табличные формы в первом контуре и как в них выглядит контроль сдачи (ячейка «сдано» на набор строк). | до пилота сбора сводок |
| N50 | К фазе 5 (ADR-0104): точность расписания алертов — пять минут (тик единого планировщика). Алерт с cron «каждую минуту» проверяется раз в пять минут. | Допущение: алерты на показатели — часовые и суточные, минутная точность им не нужна. Открыто: есть ли сценарии, где нужна проверка чаще. | до нагрузочных тестов S2 |
| N51 | К фазе 5 (ADR-0104): показатель алерта считается под правами владельца алерта. Получатель может увидеть в сообщении число, посчитанное по строкам, которых сам не видит. | Допущение: алерты заводят на показатели, открытые всей группе получателей. Открыто: не нужно ли запрещать каналы доставки, если получатель не видит показатель, или считать значение для каждого получателя отдельно. | до пилота алертов |
| N52 | К фазе 5: правила качества `in_set` и бейдж качества в каталоге (ADR-0101) собирают условие `= any(${массив})` в шаблоне `sql`, где drizzle разворачивает массив в список параметров — запрос падает. Та же ошибка встретилась в моём коде и исправлена переходом на `inArray`. | Своего кода я не касался в чужой истории. Открыто: подтвердить ошибку и исправить `quality-service.ts` (`in_set`, `statuses`) тем же способом; интеграционного теста на `in_set` сейчас нет. | до приёмки фазы 5 |
| N65 | К фазе 5 (ADR-0113): нужны ли ответы из почты в обсуждение документа. `14-automation-integrations.md` §5 помечает это «опц.»; сейчас почта работает в одну сторону — письмо становится черновиком входящего. | Обратный путь требует адреса вида `doc+<id>@…` в `Reply-To` исходящих писем, разбора этого адреса на входе и правила «кто может писать в обсуждение письмом» (адрес отправителя — слабое удостоверение). Открыто: нужен ли такой путь на пилоте и допустимо ли считать совпадение адреса достаточным подтверждением личности. | до пилота канцелярии |
| N66 | К фазе 5 (ADR-0113): что делать с письмом от неизвестного корреспондента. Сейчас черновик заводится без корреспондента, а очередь показывает «не найден по адресу — заведите корреспондента»; заводит его делопроизводитель вручную при регистрации. | Автоматическое заведение корреспондента по адресу засоряло бы справочник (рассылки, личные ящики, опечатки в домене). Открыто: заводить ли корреспондента автоматически для адресов из белого списка доменов ведомств и нужно ли сопоставление по домену, а не по полному адресу (`@mvd.example.tj` → «МВД»). | до пилота канцелярии |
| N67 | К фазе 5 (ADR-0113): сколько хранить записи очереди «Из почты» и тексты писем. Сейчас записи живут вечно: по ним видно, что пришло и почему не стало документом. | Текст письма ограничен 100 000 символов, вложения лежат обычными файлами документа и живут по его правилам. Открыто: нужна ли чистка отклонённых и неразобранных писем по сроку (например, 180 дней) и нужно ли прятать текст письма от тех, кто не видит заведённый из него документ. | до пилота канцелярии |
| N68 | К фазе 5 (ADR-0112): держать ли сервер документов ONLYOFFICE в контуре организации и в какой редакции. Реализация рассчитана на самостоятельное развёртывание (профиль `office`); Community Edition ограничивает число одновременных подключений. | Пока — Community Edition в контуре организации, без выхода в интернет. Открыто: ожидаемое число одновременно редактирующих и нужна ли лицензия Enterprise; облачная версия ONLYOFFICE не рассматривается — содержимое документов не должно покидать контур. | до пилота документооборота |
| N69 | К фазе 5 (ADR-0112): что делать с файлами под грифом от «конфиденциально». Сейчас они во внешнем редакторе не открываются вовсе — исходник наружу не уходит (ADR-0085). | Правка такого файла идёт прежним путём: скачать, поправить, загрузить новую версию. Открыто: допустимо ли редактировать документы с грифом «для служебного пользования» и выше на сервере документов, развёрнутом в том же защищённом контуре, и нужен ли для этого отдельный экземпляр сервера. | до пилота документооборота |
| N70 | К фазе 5 (ADR-0112): нужна ли блокировка файла на время совместной правки. Сейчас блокировка (`locked_by`) ставится вручную и переводит чужой редактор в режим просмотра, но сам редактор её не ставит: несколько человек правят один документ одновременно — в этом смысл совместного редактирования. | Конфликт версий не теряет правку: она ложится следующей версией с отметкой, поверх какой сохранена. Открыто: должна ли карточка документа показывать «файл сейчас правят» (сессия редактора видна в базе) и нужно ли запрещать загрузку новой версии обычным путём, пока редактор открыт. | до пилота документооборота |
| N53 | К фазе 5 (пайплайны, ADR-0106): что считать геокодированием. Сейчас шаг `geocode` сопоставляет поле со справочником территорий по коду или точному названию и добавляет идентификатор территории и точку её центра; разбора адресов («ул. Рудаки, 12») нет. | Для сводок по территориям этого достаточно, а нечёткое сопоставление без внятного порога даёт молчаливо неверные привязки. Открыто: нужен ли разбор адресов, по какому справочнику адресов (его в стране может не быть) и что делать со строками, которые не сопоставились, — отбрасывать, помечать или ронять прогон. | до пилота с адресными данными |
| N54 | К фазе 5 (ADR-0106): нужен ли свой SQL в середине цепочки. Сейчас `custom_sql` — шаг-источник: только первый и единственный, дальше преобразования делает второй пайплайн поверх результата. | Так сырой SQL остаётся на единственном пути (разбор `libpg-query` + роль `kchs_query`), а компиляция цепочки — синхронной. Открыто: достаточно ли этого аналитикам или стоит принять сырой SQL промежуточным источником `QuerySpec` (тогда компилятор запросов станет асинхронным, а проверку переписанного запроса придётся делать дважды). | по отзывам аналитиков на пилоте |
| N55 | К фазе 5 (внешние БД, ADR-0107): чем ограничивать запрос во внешней базе. Запрос пользователя выполняется там целиком под учётной записью подключения; платформа проверяет только имена (схема, таблица, поле-курсора) и подставляет значение курсора параметром. | Ограничивать должна учётная запись подключения — только чтение и только нужные таблицы; об этом сказано в подсказке экрана. Открыто: нужен ли платформе свой разбор запроса к чужой СУБД (у каждой свой диалект) и потолок времени/строк на стороне источника, помимо наших тайм-аутов. | до подключения боевой базы заказчика |
| N56 | К фазе 5 (ADR-0107): какие ещё СУБД нужны. Реализованы PostgreSQL и MySQL/MariaDB; MS SQL и ClickHouse из `06-analytics-engine.md` §2 — нет. | Вид интеграции добавляется перечислением и одним драйвером, поэтому расширение ничего не ломает. Открыто: какие СУБД реально стоят у заказчика (1С обычно на MS SQL или PostgreSQL) и нужна ли ClickHouse до колоночного tier. | до пилота интеграций |
| N57 | К фазе 5 (внешние ГИС-службы, ADR-0108): откуда администратор берёт названия слоёв и наборов матриц. Сейчас `LAYERS`, `LAYER`, `TILEMATRIXSET` и `typeName` вводятся руками по документации службы. | Ошибка видна сразу: «Проверить соединение» запрашивает тайл или первую страницу объектов. Открыто: делать ли разбор `GetCapabilities` с подсказкой списка (у WMS, WMTS и WFS это три разных XML-схемы и три разных набора версий). | до подключения гос. служб на пилоте |
| N58 | К фазе 5 (ADR-0108): кто отвечает за то, что внешняя служба показывается всем сотрудникам. Слой-ссылка — объект установки с `everyone:view`; политик строк у чужих данных нет и быть не может. | Заводит и выключает службы владелец способности `gis.basemaps.manage` — решение принимает администратор ГИС, а не автор карты. Открыто: нужны ли службы с ограниченной видимостью (по пространствам или ролям) и надо ли отмечать их гриф, если служба отдаёт чувствительные данные. | до подключения служб с ограничением доступа |
| N59 | К фазе 5 (ADR-0110): вес отложенной сборки deck.gl. `MVTLayer` живёт в бочке `@deck.gl/geo-layers`, которая тянет `h3-js` и `@loaders.gl/wms` (они не используются), а подпутей пакет не публикует — отложенный кусок 507 КБ gz вместо примерно 150 КБ. | Оставлено как есть: кусок грузится только на карте со слоем сверх порога, основная сборка не изменилась. Варианты на будущее: свой тайловый слой на `@deck.gl/core` + `@deck.gl/layers` + `@loaders.gl/mvt` или ожидание подпутей в deck.gl. | до нагрузочных тестов S2 |
| N60 | К фазе 5 (ADR-0110): значение порога deck.gl по умолчанию — 50 000 объектов слоя. | Порог настраивается администратором (раздел «Базовые карты»); 50 000 выбрано по тому, где MapLibre на тайлах начинает заметно проседать на обычной рабочей машине. Открыто: подтвердить значение на данных установки. | до пилота фазы 5 |
| N61 | К фазе 5 (ADR-0110): в deck.gl уходят только простые роли стиля (заливка, линия, точка-кружок). Слой с кластерами, подписями, тепловой картой или точками-значками остаётся в MapLibre независимо от размера. | Так стиль остаётся одним и тем же, а легенда не расходится с картой. Открыто: нужны ли подписи и кластеры в ускоренной отрисовке — это отдельные слои deck.gl и своя ветка легенды. | до приёмки фазы 5 |
| N62 | К фазе 5 (ADR-0109): порог колоночного tier — 1 млн строк, и копия собирается сама при новой версии данных крупного датасета. | Порог и выключатель — в администрировании; сборка 5 млн строк занимает около 30 с и 83 МБ в хранилище. Открыто: подтвердить порог и нужно ли ограничить время суток для пересборки копий больших датасетов. | до пилота фазы 5 |
| N63 | К фазе 5 (ADR-0109): денежные и точные числа колоночная копия хранит как `DECIMAL(38,12)`, а сам Postgres допускает `numeric` без ограничений. Значение с 27 и более цифрами до запятой в копию не поместится. | Сборка такой копии завершится ошибкой, датасет останется на Postgres-пути — данные не портятся. Открыто: нужны ли такие величины в датасетах установки. | до загрузки реальных данных |
| N64 | К фазе 5 (ADR-0109): колоночная копия не хранит геометрию, поэтому пространственные запросы и карты всегда считает Postgres. | Для карт это и не нужно: тайлы отдаёт PostGIS. Открыто: нужен ли DuckDB spatial для тяжёлого пространственного анализа (07-gis-engine.md §15, сценарий S2). | до нагрузочных тестов S2 |
| N71 | К фазе 5 (ADR-0118): на какой кластер рассчитывать чарт S2. Сейчас предполагается, что администратор сам ставит контроллер Ingress, cert-manager, KEDA, Prometheus и оператор Postgres, а чарт знает о них только адреса и имена классов. | Так чарт не навязывает состав платформы и работает в закрытом контуре. Открыто: какой дистрибутив Kubernetes и какие из этих компонентов уже есть у заказчика — от этого зависит, нужны ли в чарте готовые наборы значений под конкретную платформу и не проще ли заменить KEDA на то, что уже стоит. | до проектирования стенда S2 |
| N72 | К фазе 5 (ADR-0118): порог масштабирования воркера — 50 ожидающих заданий на реплику, движка — 5. | Числа взяты из соотношения «дешёвые задания ядра (уведомления, индекс) против минутных заданий движка (импорт, отчёт, расшифровка)», а не из замера. Открыто: подтвердить их на стенде S2 и решить, не нужны ли разные пороги по очередям (импорт и печать отчётов ведут себя по-разному). | до нагрузочных тестов S2 |
| N73 | К фазе 5 (ADR-0118): встроенные Postgres, Redis, MinIO и Meilisearch в чарте — один экземпляр без репликации, они объявлены режимом стенда. | Промышленная установка выключает их и получает адреса управляемых служб. Открыто: не стоит ли запретить их при `NODE_ENV=production` совсем, чтобы «стенд» не переехал в эксплуатацию незаметно, и достаточно ли для этого предупреждения в NOTES. | до первой установки S2 |
| N74 | К фазе 5 (ADR-0118): хранилище публикуется отдельным именем, и браузер ходит в него напрямую по подписанным ссылкам. В кластере это значит второй Ingress (или адрес облачного S3) и второй сертификат. | Иначе подпись SigV4 не сойдётся по заголовку `Host` (ADR-0044), а весь файловый трафик пошёл бы через api. Открыто: устраивает ли службу эксплуатации второе публичное имя и не нужен ли путь «всё через один домен» (S3 за тем же Ingress по префиксу) для контуров с одним сертификатом. | до проектирования стенда S2 |
| N75 | К фазе 5 (ADR-0120): чарт проверяется статически (`helm lint`, `helm template`, `kubeconform` на трёх наборах значений); установка в настоящем кластере откладывается до стенда S2. | У исполнителя кластера нет, а поднимать его рядом с общим стендом разработки рискованно. Открыто: нужно ли ночное задание с `kind` рядом со `stack.yml` (оно тянет сборку четырёх образов, включая движок на 4 ГБ) или достаточно проверки при установке стенда S2. | до приёмки фазы 5 |
| N76 | К фазе 5 (ADR-0119): профили S2 берут масштаб «1000 одновременных сотрудников» как утроенный предел S1 и 200 тайлов в секунду как порог из `15-admin-operations.md` §8. | Это ориентиры проектной документации, а не ожидания заказчика. Открыто: сколько сотрудников и какая доля работы с картами ожидаются в первом контуре S2 — от этого зависят и цифры профилей, и размер кластера. | до заказа оборудования S2 |
