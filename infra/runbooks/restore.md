# Восстановление из резервной копии

Цель: RTO ≤ 1 час, RPO ≤ 15 минут (`docs/02-architecture/01-overview.md`).

## Что резервируется

| Данные | Способ | Периодичность |
|---|---|---|
| PostgreSQL | `pgBackRest`: полный — еженедельно, инкрементальный — ежедневно, WAL — непрерывно | RPO 15 мин |
| Объектное хранилище (MinIO) | версионирование бакетов + репликация | непрерывно |
| Meilisearch | перестраивается из Postgres (`search.reindex`) | не резервируется |
| Redis | только кэш и очереди; AOF для очередей | не резервируется |

## Порядок восстановления

1. **Остановить приложение**, оставив базу и хранилище:
   ```bash
   docker compose --profile app stop api worker engine web
   ```
2. **Восстановить кластер Postgres** на нужный момент времени:
   ```bash
   pgbackrest --stanza=kchs --type=time --target="2026-09-17 08:00:00+05" restore
   docker compose up -d postgres
   ```
3. **Проверить схему и целостность**:
   ```bash
   pnpm db:migrate                 # применит недостающие миграции, если бэкап старее релиза
   docker compose exec postgres psql -U postgres -d kchs -c "select count(*) from objects;"
   ```
4. **Восстановить файлы**, если утрачено и хранилище: вернуть бакеты
   `kchs-files`, `kchs-media`, `kchs-previews` из реплики или снимка.
5. **Перестроить поисковый индекс** — он не резервируется:
   ```bash
   curl -X POST http://localhost:3000/api/v1/admin/engine/echo  # проверка очередей
   # либо разово: задание index/search.reindex через экран «Процессы»
   ```
6. **Поднять приложение** и проверить здоровье:
   ```bash
   docker compose --profile app up -d
   curl -fsS http://localhost:3000/health
   ```
   На экране «Администрирование → Здоровье системы» все компоненты должны быть
   «Работает», очередь outbox — без отставания.
7. **Зафиксировать инцидент**: журнал аудита сохраняется вместе с базой; проверить,
   что записи за период до сбоя на месте.

## Проверка процедуры

Тестовое восстановление выполняется ежемесячно на отдельном стенде и фиксируется
в `docs/progress.md` (раздел «Результаты проверок»).
