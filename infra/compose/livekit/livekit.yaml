# LiveKit SFU встреч (ADR-0089). Ключи — из окружения (LIVEKIT_KEYS в compose,
# значения из .env), поэтому в файле их нет.
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: false
turn:
  enabled: true
  udp_port: 3478
logging:
  level: info
